# Valoura — Context-Specific Tech Stock Valuation Engine

> ## SESSION HANDOFF — state as of 2026-08-11 (Backtest #1 COMPLETE, steps 1–7)
>
> **Where things stand:** the engine classifies **457 tickers**. A separate
> **backtest universe** of 11,009 candidates exists (`universe` table), with
> **502 flagged `in_backtest`** (488 active + 14 human-reviewed delisted).
> **All seven steps of `~/Downloads/CVE_Backtest_01_Plan.md` are done.**
>
> **THE HEADLINE: the engine passes Test 1 and fails everything else.**
> Pass conditions: **#1 PASS** (10 of 11 cells, undervalued beats overvalued on
> median), **#2 FAIL** (the portfolio loses to QQQ and to its own equal-weight
> universe at 5, 10 and 20 positions), **#3 partial** (holds in 2 of 3 regimes
> on the panel, 1 of 3 in the portfolio), **#4 PASS on the panel** (signal in 10
> cells) but fails in the portfolio (only 5 of 20 cells ever fire). Test 2 —
> billed as the framework's strongest claim — is a **clean negative**.
>
> Read that as: the *verdict* carries modest real signal on the full panel; the
> *portfolio rules built on it* destroy that signal through turnover, selection
> and concentration. The honest next move is to fix the rules, not to re-run.
>
> **A data defect was found and fixed mid-session** — stock splits corrupted
> stored price history (KLAC 10x, CRWD 4x). One reported finding was wrong
> because of it and has been corrected in place. See *Price integrity defect*.
> `price_integrity_check.py` should be run after every backfill from now on.
>
> Steps 1–2 detail:
>
> - **Part A verification: all three checks PASS.** A1 depth — NVDA/MSFT/INTC
>   hold 81 statement quarters (2006→) + 109–122 EARNINGS quarters, so the
>   2012 start needs no shortening. A2 delisted availability — AV returned full
>   statements *and* adjusted prices for every probed delisted name including
>   LN (delisted 2020), better than the 99%-empty probe history predicted; the
>   D3 survivorship bound is therefore a disclosure, not a fallback. A3 filing
>   dates — 92% of statement rows carry a true `reported_date`, EARNINGS 100%,
>   uniform 87–92% per year across the window.
> - **Backfill complete for all 502 `in_backtest` names.** `fundamentals`
>   55,759 → **113,763 rows** (107,303 with a filing date); `price_data` **503
>   tickers, 2.21M bars, 1962→2026**. 410 OK, 2 partial, **zero failures**,
>   1,662 AV calls. SPY added (1993→) — it was missing entirely; QQQ 1999→.
> - **Rule 1 IMPLEMENTED and verified** (was the blocker). Two layers: bars
>   after `delisting_date` are dropped at ingest in
>   `backfill_fundamentals.py::_backfill_price_history_av`, and
>   `backtester.py::get_price_on_date` returns None past that date. DB-wide
>   assertion returns **0** rows past any delisting date. All 14 delisted names
>   now carry fundamentals + prices ending exactly on their last trading day.
> - **11 names (2.2%) have data gaps** and will drop out at panel build —
>   listed in `backtest_data_gaps.tsv`. 9 lack a statement type (mostly recent
>   micro-cap IPOs); ANSS and OZON have full statements but no price series
>   (ANSS acquired by Synopsys; OZON has no yfinance coverage).
>
> **Standing decision — panel BM axis = static current classification.** Each
> ticker keeps its present-day engine BM across all historical quarters;
> `fh_stage` is recomputed point-in-time per quarter. Rationale: the LLM
> tiebreaker is non-deterministic and too costly per stock-quarter, and
> re-running only the rule validators would diverge from the engine actually
> being tested. The mild hindsight bias **must be disclosed** in the Part E
> Limitations panel.
>
> **Read these three first:**
> 1. *AV Data Defects* (below) — nine ways Alpha Vantage returns plausible-looking
>    wrong data. Non-optional before touching AV.
> 2. `BACKTEST_RULES.md` — Rule 1 now implemented; verification results recorded
>    there. Open items A (ticker reuse) and B (survivorship scope) still stand.
> 3. *Backtest #1 — Progress & Results* (below), then *Known Open Items*.
>
> **Immediate next steps (Backtest #2 — start a FRESH session):**
> 1. **Regime-condition the fair ranges.** The single clearest finding: bands
>    are absolute, so 75.6% of 2012 rows read undervalued vs 43.1% of 2026 rows.
>    The engine is partly detecting the calendar, not cheapness.
> 2. **Replace the portfolio rules.** Sell-on-falling-stage is unsupported
>    (Test 2) and costly; the depth ranking underperforms random selection.
>    Test: hold-until-overvalued, 10+ positions, and a within-cell rank.
> 3. **Fix the `ev_ntm_arr` key bug** so the app and the panel agree.
> 4. Only then re-run. Do not re-run the same rules on the same data.
>
> **Environment gotcha:** use **`/usr/bin/python3`** — it is the only interpreter
> with yfinance + google.generativeai + supabase. Plain `python3` is 3.14 and
> lacks yfinance. Credentials load from `~/.zshrc`, which does **not** propagate
> into Bash subshells; the scripts parse it directly.
>
> **Working directory:** `/Users/ommelwani/gemini/antigravity/scratch/CVE_Engine`
> (moved and renamed 2026-08-16; note *not* `.gemini` — an older path in some
> docs). No space in the name, so no quoting needed. Scripts self-locate via
> `os.chdir(os.path.dirname(os.path.abspath(__file__)))` and run correctly from
> anywhere. `oasisdilemna`, `productivity-suite`, `smart-switch-engine` and the
> emulator `.ini` files stayed in `scratch` — they are not CVE. `.claude/` also
> stayed (it configures the scratch root); its `launch.json` cve-app entry
> points at `CVE_Engine/Valuora_Previous_UI/app.py`.


## Project Purpose
Valoura — shipped as the **Contextual Valuation Engine (CVE)** — is a
context-specific stock valuation algorithm for technology stocks. Every ticker is
classified along three independent axes, and valuation logic is applied
conditionally based on the resulting cell.

Two halves:
1. **Offline pipeline** (`data_pipeline1.py`) — ingests fundamentals, computes
   metrics, classifies, values, and writes to local SQLite. Re-run as thresholds
   are calibrated.
2. **Web app** (`Valuora_Previous_UI/app.py`) — Streamlit front end with Supabase
   accounts, per-user watchlists, and stage-change email alerts. Reads the SQLite
   DB; never writes to it.

NOT financial advice. NOT a live trading system.

---

## The Three-Classifier Architecture

Classifiers are deliberately independent — combining them loses signal.

---

### Classifier 1 — Business Model (BM)
Five mutually exclusive categories evaluated in strict priority order. First match wins.

| Priority | Category | Key Signal |
|----------|----------|------------|
| 1 | `saas` | subscription ≥70%, gross margin ≥65%, deferred revenue ratio ≥5%, no owned infra, hardware <10% |
| 2 | `hyperscale` | owns infra = yes, usage-based ≥25%, gross margin ≥55% |
| 3 | `semi_hardware` | hardware ≥35% OR (gross margin <55% AND ad revenue <40%) |
| 4 | `consumer_internet` | advertising ≥40% (POSITIVE rule, not catch-all) |
| 5 | `deep_tech` | no dominant revenue stream ≥40%, or pre-revenue |

**Classification method** — hybrid rule-based + LLM:
- Rule-based balance-sheet validators run first (one per category)
- LLM (Gemini) called ONLY to break ties: 2+ validators pass, OR 0 pass, OR confidence < 0.80
- `MANUAL_BM_OVERRIDES` short-circuits both validators and LLM (used when AV data
  structurally can't pass a correct validator — e.g. MSFT PPE not exposed by AV,
  or when an LLM tiebreaker lands on a category that contradicts the company's
  actual business model). 19 entries as of 2026-08:
  ```python
  MANUAL_BM_OVERRIDES = {
      # Infra owners AV can't validate (PPE not exposed)
      "MSFT": "hyperscale", "AMZN": "hyperscale", "GOOGL": "hyperscale",
      "ORCL": "hyperscale", "IBM": "hyperscale",
      # "Neocloud" AI hyperscalers — own GPU data centres, usage-based revenue.
      # Validators read their SaaS-like margins and miss the CapEx intensity.
      "CRWV": "hyperscale", "NBIS": "hyperscale",
      # Subscription software the validators/LLM mislabelled
      "SNOW": "saas", "MDB": "saas", "DOCN": "saas", "U": "saas",
      # Semiconductor / hardware names that LLM tiebreaks sent to deep_tech
      "INTC": "semi_hardware", "ADI": "semi_hardware", "NTAP": "semi_hardware",
      # Consumer platforms mislabelled deep_tech (AR/gaming/gig R&D signals)
      "DASH": "consumer_internet", "SNAP": "consumer_internet",
      "TTWO": "consumer_internet",
      # Frontier R&D with zero-inventory/zero-PPE profiles that look SaaS-like
      "QBTS": "deep_tech", "BKSY": "deep_tech",
  }
  ```
  **Override policy:** always review a `LOW`-confidence result, and override when
  the assigned category contradicts the company's primary business model. A LOW
  confidence score alone is not grounds for an override — the category has to be
  wrong. (See *Known Open Items* for the deferred hyperscale category split.)
- `bm_method` values logged per classification: `validator`, `llm_tiebreaker`,
  `override`, `llm_post_saas_demotion_zero_pass`

**SaaS post-classification gate** — a ticker classified `saas` by validators or LLM
must additionally clear ONE of:
1. `deferred_revenue_ratio >= 0.05` (5% of LTM revenue — billed-in-advance proxy)
2. ARR value present in `manual_metrics.json`
3. Listed in `MANUAL_BM_OVERRIDES` as "saas"

Failure demotes to next best non-saas category (LLM re-called on remaining
candidates). This prevents ad-driven or hardware companies riding a loose SaaS label.
Threshold 5% separates true SaaS (CRM/NET/ADBE/SNOW at 33-72%) from zero-deferred
companies like RDDT (1.1%) that got saas from the LLM.

---

### Classifier 2 — Financial Health (FH) Stage (1–4)
Each metric independently maps to a stage 1–4, then weighted-averaged. Result
rounded to nearest integer → `fh_stage`.

**Stage definitions (anchor = FCF margin adjusted for SBC):**
| Stage | Name | FCF margin adj |
|-------|------|----------------|
| S1 | Cash burning | < -15% |
| S2 | Growth investing | -15% to +8% |
| S3 | FCF positive | +8% to +22% |
| S4 | Cash compounding | > +22% |

**HARD CAP:** if FCF margin adj = Stage 1, overall stage cannot exceed Stage 2
regardless of other metrics. Prevents high-GM companies burning heavily on S&M
from being misclassified as healthy.

**Weights (must sum to 100% per column):**
| Metric | S1 | S2 | S3 | S4 |
|--------|----|----|----|-----|
| FCF margin adj | 30 | 30 | 30 | 35 |
| Cash runway | 45 | 15 | 0 | 0 |
| Gross margin | 15 | 15 | 15 | 15 |
| Operating leverage | 0 | 25 | 20 | 10 |
| SBC % revenue | 5 | 10 | 10 | 5 |
| Debt/equity | 5 | 5 | 10 | 15 |
| ROIC | 0 | 0 | 15 | 20 |

**Data quality flags:**
- `net_income_quality_flag=1` fires when `NI` diverges materially from
  `OpInc*(1-tax_rate)` — signals non-cash items (goodwill, IP gains) distorting NI.
  Affects MRVL (large non-cash gain → cycle P/E inflated to 3000+x).
- `excluded_currency`: tickers where `yfinance.financialCurrency` is not USD are
  fully skipped (TSM=TWD, ASML=EUR). Currency authority is yfinance, NOT AV.
- Negative book equity: `debt_equity` returns None when equity ≤ 0 (buyback-heavy
  mature companies). Score neutral, do not penalise.
- `MIN_PRIOR_BASE_REVENUE = $10M`: growth rates nulled out when prior-LTM revenue
  below threshold (JOBY went $111k→$77M = 69,874% — technically correct, useless).

---

### Classifier 3 — Behavioural Sentiment (not yet implemented)
Planned dual sub-score (0-10 each), kept separate:
- Hype vs fundamentals (valuation stretch, momentum, retail attention, insider selling)
- Analyst perspective (consensus, price targets, revision momentum, surprise history)

Combined into a 2×2 tension matrix: low-hype+bullish = entry; high-hype+bullish =
hold only; low-hype+weak = watch; high-hype+weak = avoid.

---

## Valuation Matrix (20 cells = 5 BM × 4 FH stages)

**Current version: `MATRIX_VERSION = "v2-2026-08"`** (constant in `data_pipeline1.py`).
Written into every `valuations.valuation_json` row so backtest output is traceable
to the matrix revision that produced it. Bump it whenever a range or primary
metric changes.

Two sources of truth, kept in sync:
- `VALUATION_MATRIX` (`data_pipeline1.py`) — routing string per cell; decides
  which multiples get computed and stored.
- `FAIR_RANGES_FULL` (`Valuora_Previous_UI/app.py`) — ordered metric list per
  cell with fair ranges. **First entry is the PRIMARY metric** and drives the
  verdict; the rest are secondary qualifiers.

| Cell | Primary (drives verdict) | Secondary |
|------|--------------------------|-----------|
| hyperscale-1 | EV/NTM Rev 6–15x | Rule of 40 0–20% |
| hyperscale-2 | EV/NTM Rev 3–12x | — |
| hyperscale-3 | CapEx EV/EBIT 20–35x | PEG 0.5–1.5x · R40 25–45% |
| hyperscale-4 | FCF Yield 2.0–4.0% | PEG 0.5–1.5x · R40 30–50% |
| saas-1 | EV/NTM Rev 8–20x | Rule of 40 0–20% |
| saas-2 | EV/NTM ARR 8–18x | Rule of 40 15–35% |
| saas-3 | EV/FCF 25–45x | PEG 0.8–2.0x · R40 30–50% |
| saas-4 | FCF Yield 2.0–3.5% | EV/FCF 20–35x · PEG 0.5–1.5x · R40 40–60% |
| semi_hardware-1 | EV/NTM Rev 0.5–2.0x | — |
| semi_hardware-2 | EV/NTM Rev 1.5–4.0x | — |
| semi_hardware-3 | Cycle P/E 20–35x | PEG 0.5–1.5x |
| semi_hardware-4 | FCF Yield 2.5–4.5% | PEG 0.5–1.2x |
| consumer_internet-1 | EV/NTM Rev 1–5x | — |
| consumer_internet-2 | EV/NTM Rev 4–10x | Rule of 40 10–30% |
| consumer_internet-3 | EV/EBITDA 18–30x | PEG 1.0–2.5x · R40 20–40% |
| consumer_internet-4 | **P/E 15–25x** | EV/EBITDA 12–18x · PEG 0.8–1.5x |
| deep_tech-1 | EV/NTM Rev 20–60x | — (EV/Cash fallback, see below) |
| deep_tech-2 | EV/NTM Rev 5–12x | — |
| deep_tech-3 | EV/GP 15–30x | Rule of 40 10–30% |
| deep_tech-4 | FCF Yield 2.0–4.0% | PEG 0.5–1.5x |

### v2 change log (from v1)
- **hyperscale-2:** CapEx EV/EBIT 40–80x removed → EV/NTM Rev primary. The
  CapEx-adjusted lens was structurally wrong at S2 where depreciation dominates.
- **hyperscale-2 floor 6x → 3x (v2.1):** at a 6x floor, mature hyperscalers
  scored *Undervalued* on scale rather than cheapness (AMZN 3.4x, IBM 3.6x).
  Both now read *Fair*.
- **consumer_internet-4:** P/E added as PRIMARY ahead of EV/EBITDA.
- **deep_tech-1:** `P/S` renamed and re-keyed to `EV/NTM Rev` (same 20–60x band).
  **No `P/S` string remains in that cell** — verdict and source-label lookups key
  on the metric-name string, so the rename had to be total.
- **semi_hardware-3:** Rule of 40 dropped from fair ranges (Cycle P/E + PEG only).
- **S4 FCF-yield retune:** hyperscale-4 2.0–4.0, saas-4 2.0–3.5,
  semi_hardware-4 2.5–4.5, deep_tech-4 2.0–4.0 (+PEG).

### deep_tech-1 pre-revenue fallback
EV/NTM Rev divides by ~zero for pre-revenue names. When `revenue` is null or ≤ 0
at deep_tech-1, the cell switches to:
- `primary_method = "EV/Cash and Cash Runway"`
- `ev_to_cash = enterprise_value / cash_and_equivalents`
- `metric_label = "EV/Cash"` (so the UI labels it correctly)
- `cash_runway_months` surfaced as the supporting signal

This is the zero-division guard for pre-revenue names. `EV/Cash` routes through
the **ratio** branch of `get_verdict()` (lower = cheaper), as do `Cycle_PE` and
all EV/x multiples; `FCF_yield` routes through the **yield** branch
(higher = better).

### FLAG_peg_value_trap
Computed wherever PEG is a cell metric. Fires when **PEG < 0.8 AND LTM YoY
revenue growth is below the prior quarter's** (decelerating). Writes
`FLAG_peg_value_trap` into `valuation_json` with the note *"Low PEG with
decelerating growth — may indicate a value trap rather than undervaluation."*
and surfaces it in the app's Risk Flags panel. The PEG value itself is never
suppressed — both are shown. Mirrors the RPO two-quarter-persistence idea:
direction matters, not the level alone.

### Valuation matrix — general rules
- PEG applies at S3 and S4 only; never S1/S2 (undefined on near-zero earnings)
- R40 (Rule of 40 = revenue growth + FCF margin) is context, never a primary multiple
- Zero-division guard mandatory: `value = num/denom if denom and denom != 0 else None`
  Return None and display "N/A — pre-revenue". Never crash.

---

## RPO Qualifier System

RPO (Remaining Performance Obligations) is an ASC 606 accounting disclosure —
formal measure of contracted future revenue not yet recognised.

**Extraction:** `metrics_extractor.py` — keyword-window approach for 10-Qs/10-Ks.
Searches for RPO section using `_RPO_KEYWORDS`, extracts ±3,000 chars around each
match, up to 40,000 chars total. Avoids blind head-truncation that missed Notes
sections in large (8-31MB) SEC HTML filings.

Keywords searched:
```
"remaining performance obligation"
"remaining performance obligations"
"transaction price allocated to remaining"
"unsatisfied performance obligation"
```

**Metrics computed** (stored in `computed_metrics`):
- `rpo` — current-period RPO (from `manual_metrics.json`)
- `rpo_prior_year` — same-quarter prior year RPO
- `rpo_growth_yoy` — `(rpo / rpo_prior_year - 1)` × 100 (%)
- `rpo_revenue_spread` — `rpo_growth_yoy - revenue_growth_yoy` (percentage points)

**Qualifier states:**
| State | Condition | Meaning |
|-------|-----------|---------|
| `forward_demand_ahead` | spread > +5pp | RPO outpacing revenue — contracted backlog building |
| `neutral` | -5pp ≤ spread ≤ +5pp, OR single-quarter negative | No decisive signal |
| `forward_demand_decelerating` | spread < 0pp for 2+ consecutive quarters | Contracted pipeline contracting |

**Qualifier cells:**
- **Primary role:** hyperscale-S2, hyperscale-S3 (RPO is critical forward demand signal)
- **Secondary role:** saas-S2, saas-S3 (supplementary context, not primary multiple)
- No role: semi_hardware, consumer_internet, deep_tech (RPO not standard in these models)

**Known state (as of 2026-05-21):**
- `rpo_prior_year` still NULL for most tickers (only 1 prior-year extraction cycle
  completed). Will self-resolve next quarter when current values become prior_year.
- PANW: -14.2pp spread, single quarter → `neutral` (not yet `forward_demand_decelerating`)
- SNOW: +13.1pp spread → `forward_demand_ahead` ✓ (primary multiple may understate)
- ADBE: +1.9pp → `neutral` (flat, no directional signal)

---

## Data Architecture

> ⚠ **Before writing any code against AV, read the *AV Data Defects* section
> below.** AV silently returns wrong-but-plausible data in nine documented ways
> (frozen delisted prices, `"None"` strings, bogus delisting dates, discarded
> fundamentals, reused tickers). Each one has cost real debugging time.

### Primary financial API — Alpha Vantage (AV)
Quarterly fundamentals: INCOME_STATEMENT, CASH_FLOW, BALANCE_SHEET, **EARNINGS**.

**EARNINGS is the only endpoint carrying a filing date** (`reportedDate`). The
three statement endpoints do not, which is why `backfill_fundamentals.py`
fetches EARNINGS first and stamps `fundamentals.reported_date` by fiscal period.
That column is what makes point-in-time backtesting honest — without it the
backtester falls back to a flat 45-day-after-fiscal-end guess.

**Field-name traps (both cost real money in wrong valuations):**
- D&A is `depreciationDepletionAndAmortization`, **not** `depreciationAmortization`.
  Reading the wrong name silently returns 0, collapsing EBITDA into operating
  income and understating every EV/EBITDA cell.
- Historical shares outstanding are in the BALANCE_SHEET as
  `commonStockSharesOutstanding`. Use these for point-in-time market cap / EV —
  `backtester.py` previously used *today's* share count for all history.
- The live pipeline caps fundamentals at 20 quarters (`YEARS_OF_DATA * 4`).
  `backfill_fundamentals.py` deliberately does **not**, and pulls the full
  history AV holds (~81 quarters for mature names).

**Tier: PRO ($50/month) — 75 req/min, NO daily cap.** Active since 2026-08.

```python
AV_CALLS_PER_MINUTE = int(os.getenv("AV_CALLS_PER_MINUTE", "75"))  # Pro is the default
AV_SLEEP = 60.0 / AV_CALLS_PER_MINUTE                              # 0.8s at 75/min
```

No env export is required — the engine defaults to Pro. `~/.zshrc` also sets
`AV_CALLS_PER_MINUTE=75` explicitly. Override the env var only if AV changes the
plan ceiling.

**All 25/day free-tier handling has been REMOVED.** The `AV_PRO_TIER` flag is
deleted; there is no daily-quota logic anywhere in the codebase. The only limit
that exists on Pro is the per-minute burst ceiling.

**Rate-limit handling (`_av_fetch`):**
- AV returns HTTP 200 with an `"Information"` or `"Note"` key when the per-minute
  burst ceiling is hit. Hard errors (`"Error Message"`, delisted ticker) are
  distinct and never retried.
- Rate-limit response → **unconditional** exponential backoff (15s / 30s / 60s),
  retrying up to `AV_RATE_LIMIT_RETRIES = 3` times before returning `None`.

**`AV_LAST_FAILURE_REASON`** (module-level in `data_pipeline1.py`) records *why*
the most recent fetch returned nothing, so callers stop guessing:
`rate_limit | api_error | timeout | exception | empty | None`.
Set on every `_av_fetch` failure path and on the empty-`quarterlyReports` path;
read by `run_universe.py` to label failures honestly. Before this existed, any
zero-record fetch was reported as "rate-limit/quota" — which on Pro misattributed
delisted symbols (JNPR acquired by HPE, ZI reticked to GTM, PSTG not found) as
throttling.

**Consecutive-failure stop (`run_universe.py`):** 5 consecutive zero-record
tickers → stop. This is a **structural** failure detector only (dead API key,
bad symbols, network down). Per-minute bursts are absorbed by the `_av_fetch`
retry and never reach the counter.

**Measured Pro throughput:** 16 tickers / 38 AV calls in **115 seconds**, zero
rate-limit responses. The same work took ~2 days on the free tier.

**Other key behaviours:**
- `INSERT OR IGNORE` on all fundamentals rows — idempotent, safe to re-run
- Per-statement skip check: if rows already stored for that ticker+statement, AV
  call is skipped (crucial for partial-load recovery, e.g. INTC IS already stored)
- CFLT removed from universe (taken private 2026 — AV returns 0 records, yfinance
  returns "symbol may be delisted")

### Price data — yfinance
- 5-year daily OHLCV via `yf.download(period="5y")`
- `financialCurrency` field is the currency authority for non-USD exclusion gate
- NOT for commercial use — must migrate in production

### SEC EDGAR (RPO only — Prong 2)
- CIK resolved from `company_tickers.json` (SEC bulk feed, cached locally)
- CRITICAL: accession number prefix ≠ company CIK (prefix is filing agent's CIK
  e.g. 1193125 = Edgar Online). Always look up via `_load_ticker_cik_map()`.
- `sec-edgar-downloader` fetches 10-Qs and 10-Ks into local filing cache
- `fetch_10q_text()` accepts `target_keywords` parameter → keyword-window extraction

### Manual/Automated KPI extraction — metrics_extractor.py (Prong 3)
Extracts: `rpo`, `rpo_prior_year`, NRR, ARR, MAU, ARPU, TCV from 8-K press
releases (primary) and 10-Q fallback.
- Confidence < 0.80 → `needs_review=True`, written to `low_confidence_extractions`
- `rpo_prior_year == rpo` → duplicate sanity check → flag for review
- Output written to `manual_metrics.json`:
  `{ticker: {metric: {value, unit, period, confidence, source_url, needs_review}}}`
- Run with: `python3 metrics_extractor.py --all --force`

---

## LLM Configuration
```python
GEMINI_MODEL_NAME = "gemini-2.5-flash-lite"   # NOT gemini-2.5-flash (burns thinking tokens)
LLM_MAX_TOKENS = 400
GOOGLE_API_KEY = os.getenv("GEMINI_API_KEY") or os.getenv("GOOGLE_API_KEY", "")
```
**Env var:** `GOOGLE_API_KEY` set in `~/.zshrc`. Both names checked for compatibility.
Always run via `zsh -c 'source ~/.zshrc 2>/dev/null; python3 ...'` to ensure the
key is available in subprocess. Direct `python3` calls get `GEMINI_API_KEY=""` and
fall back to rule-based (Gemini "unavailable" in logs).

LLM is tiebreaker only — rules run first. `fallback_used` flag logged when LLM
overrides or substitutes for rules.

---

## Database (SQLite: valoura_backtest.db)

Located at: `/Users/ommelwani/gemini/antigravity/scratch/valoura_backtest.db`

### Tables
| Table | Purpose |
|-------|---------|
| `price_data` | Daily OHLCV per ticker, adjusted close. Spans **1962-01-02 → 2026-08-10**, **503 tickers / 2.21M bars** (deepened by `backfill_fundamentals.py`; the live pipeline only stores 5y). Includes SPY (1993→) and QQQ (1999→) benchmarks. Contains **no bars past any ticker's `delisting_date`** — Rule 1 is enforced at ingest |
| `fundamentals` | Raw AV quarterly JSON per ticker/statement/fiscal_date. **113,763 rows** across 4 statement types incl. `EARNINGS`. Has a **`reported_date`** column (true filing date) — 107,303 rows populated (92% of statements, 100% of EARNINGS) |
| `computed_metrics` | Derived LTM metrics + RPO columns (see below) |
| `classifications` | BM + FH classification result per ticker/date |
| `universe` | **Backtest universe** (11,009 rows) — see *Backtest Universe* below |
| `overview_cache` | Cached AV `OVERVIEW` JSON per ticker (12,341). Makes universe rebuilds free |
| `delisted_statement_probe` | Cached "does AV still hold statements for this delisted ticker?" probe (362) |
| `valuations` | Primary multiple + full multiples JSON per ticker/date |
| `fh_stage_history` | Last 8 quarters of FH stage per ticker (trajectory timeline) |
| `metrics_extraction_log` | Audit trail for every LLM extraction attempt |
| `low_confidence_extractions` | Flagged extractions (conf < 0.80) |

### fh_stage_history
`(ticker, as_of_date, fh_stage, fh_weighted_score, fh_fcf_hard_cap_applied,
computed_at)`, PK `(ticker, as_of_date)`. Populated by
`backfill_fh_history(conn, ticker, n_quarters=8)`, which re-slices cached
fundamentals to each historical quarter end and re-runs
`classify_financial_health()` on that snapshot. **Zero AV calls** — pure recompute
from cached data. `run_classification()` refreshes it automatically for every
ticker it classifies, so the trajectory stays current with no manual step.
Bootstrap for pre-existing tickers: `python3 backfill_fh_history.py`.
Currently 747 rows.

### Supabase (user data — separate from the pipeline DB)
Pipeline data stays in SQLite. **Only user-facing data lives in Supabase**, at
`https://dhubgamhpzlygbdiarjt.supabase.co`. Schema in
`Valuora_Previous_UI/supabase_schema.sql`.

| Table | Purpose |
|-------|---------|
| `users` | Mirror of Supabase Auth users; `id` = auth uid, plus `display_name` |
| `watchlist` | Per-user tickers + `matrix_cell` / `stage_at_add` snapshot + `alert_on_stage_change` |
| `stage_change_log` | Every FH stage transition the pipeline detects |

**RLS is currently DISABLED** on all three (the app uses the anon/publishable
key; dashboard-created tables enable RLS by default, which blocks all writes
with no policies defined). Must be re-enabled with per-user policies before
opening to real users — see *Known Open Items*.

### computed_metrics — key columns
Standard: `revenue_ltm`, `revenue_growth_yoy`, `gross_margin`, `fcf_ltm`,
`fcf_margin`, `fcf_margin_adj`, `sbc_ltm`, `sbc_pct_revenue`, `ebitda_ltm`,
`market_cap`, `enterprise_value`, `deferred_revenue`, `deferred_revenue_ratio`,
`eps_ltm`, `cash_runway_months`, `roic`, `operating_leverage`

RPO columns (added via `setup_database()` migration):
`rpo`, `rpo_prior_year`, `rpo_growth_yoy`, `rpo_revenue_spread`

### Schema migration
`setup_database(conn)` applies ALTER TABLE for new columns if they don't exist.
Always call before `compute_and_store_metrics`. Safe to call on every run.

Never rename existing `computed_metrics` columns — data sources are mapped onto
the fixed schema so the classification engine works unchanged.

---

## Files

| File | Role |
|------|------|
| `data_pipeline1.py` | Core engine: ingestion, metric computation, BM+FH classification, valuation matrix, RPO qualifier, stage-change detection |
| `run_universe.py` | Durable, resumable batch runner (scratch root + repo copy — **no longer in /tmp**). Skips already-classified tickers and already-cached statements. `python3 run_universe.py [TICKERS…] [--file list.txt]` |
| `backfill_fh_history.py` | One-time bootstrap of `fh_stage_history` (zero AV calls). **Not** the fundamentals backfill — see next row |
| `backfill_fundamentals.py` | **Point-in-time history backfill.** Pulls FULL quarterly history (no 20-quarter cap) for INCOME_STATEMENT / BALANCE_SHEET / CASH_FLOW / EARNINGS, stamps each quarter with its true `reported_date` from EARNINGS, and deepens `price_data` to max history. **Active names → yfinance; delisted names → AV `TIME_SERIES_DAILY_ADJUSTED`** (yfinance returns zero bars for them), with Rule 1 truncation at `delisting_date` applied at ingest and a USD guard off cached OVERVIEW. Resumable; re-runs cost ~0 calls |
| `backtest_universe_tickers.txt` | The 502 `in_backtest = 1` tickers, generated from `universe`. Input to the backfill |
| `backtest_data_gaps.tsv` | The 11 `in_backtest` names with incomplete statements or no price series |
| `backfill_results.tsv` | Per-ticker status from the last backfill run (detail kept out of the console) |
| `universe_builder.py` | Builds the `universe` table from AV `LISTING_STATUS` CSVs + an `OVERVIEW` crawl. `--build` rebuilds the table from cache with **zero API calls** |
| `fix_delisting_dates.py` | Repairs bogus delisting dates using the frozen-price-tail detector (see *AV Data Defects*) |
| `triage_review_list.py` | Filters delisted review candidates by statement availability, then auto-triages by name |
| `build_repo_db.py` | Builds the slim DB shipped to the Streamlit repo (drops `price_data` + `fundamentals`, VACUUM: 211MB → 0.7MB) |
| `BACKTEST_RULES.md` | **Authoritative backtester rules.** Rule 1 = hard-stop at true last trading day |
| `PRE_BACKTEST_PLAN.md` | Method + findings log for the backfill and universe build |
| `metrics_extractor.py` | Automated KPI + RPO extraction from SEC filings (Prong 3) |
| `manual_metrics.json` | KPI store (auto-populated + manual review queue) |
| `Valuora_Previous_UI/app.py` | Streamlit web app (CVE) — see *Web Application* below |
| `Valuora_Previous_UI/supabase_client.py` | All Supabase I/O: auth, watchlist CRUD, stage-change log, alert emails |
| `Valuora_Previous_UI/supabase_schema.sql` | DDL for the three Supabase tables (one-time paste into the SQL editor) |
| `claude.md.txt` | This file |

**Deployment:** the repo is `valouraofficial/valoura` (GitHub), auto-deployed by
Streamlit Cloud from `main`. `data_pipeline1.py` and `valoura_backtest.db` are
mirrored into `Valuora_Previous_UI/` on every push — the app reads the committed
DB snapshot.

### Key constants in data_pipeline1.py
```python
SAAS_MIN_DEFERRED = 0.05        # SaaS gate threshold
CYCLE_ADJ_YEARS = 5             # Shiller-style P/E window for semis
MAINTENANCE_CAPEX_RATIO = 0.50  # Maintenance capex as % of total for hyperscale adj
EST_TAX_RATE = 0.21             # Used for net_income_quality_flag check only
MIN_PRIOR_BASE_REVENUE = 10_000_000   # Below this, growth rates nulled out
MATRIX_VERSION = "v2-2026-08"   # Stamped into every valuations row
AV_CALLS_PER_MINUTE = 75        # Pro tier default
AV_SLEEP = 0.8                  # 60 / 75 — seconds between AV API calls
AV_RATE_LIMIT_RETRIES = 3       # Backoff retries on per-minute throttle
AV_RATE_LIMIT_BACKOFF = (15, 30, 60)
```

---

## Ticker Universe

`TICKERS` in `data_pipeline1.py` is the original **49-name seed list**, not the
current universe. It remains the default argument for `run_universe.py` when no
tickers are passed, but 222 tickers are now classified — the extras were added by
passing them explicitly:

```bash
python3 run_universe.py CRWD DDOG FTNT MU TXN     # ad-hoc names
python3 run_universe.py --file my_tickers.txt      # or from a file
python3 run_universe.py                            # falls back to the seed list
```

The runner is resumable: it skips tickers that already have a classification and
statements that are already cached, so re-running is safe and costs nothing.

```python
TICKERS = [
    # Pure SaaS
    "PANW","CRM","WDAY","ADBE","TEAM","MDB","NET","DOCN","ZS","SNOW",
    # Semiconductor / Hardware
    "AMD","NVDA","TSM","ASML","MRVL","AVGO","QCOM","INTC","KLAC",
    # Hyperscale
    "MSFT","GOOGL","AMZN","RKLB","IBM",   # ORCL at tail (swapped for RKLB)
    # Consumer Internet
    "META","AAPL","NFLX","SPOT","PINS","SNAP","UBER","ABNB","DASH","ROKU","MTCH","RDDT",
    # Enterprise Infra / Networking
    "DELL","ANET","SMCI",
    # Deep Tech / Space
    "PLTR","PATH","IONQ","JOBY","RIVN","QBTS","RGTI","ASTS","SOUN","ORCL",
]
```

---

## Current Classification State (as of 2026-08)

> **SUPERSEDED 2026-08-11 — now 457 classified.** The counts below describe the
> 222-ticker state before the backtest expansion. Current mix: semi_hardware 152,
> saas 128, consumer_internet 109, deep_tech 53, hyperscale 8. See
> *Backtest #1 — Progress & Results*. The notes in this section on override
> policy, taxonomy gaps and exclusions all still apply.

**222 tickers classified** (was 94; a 140-name technology batch was added on
2026-08-08 — see below). The universe has outgrown the static `TICKERS` list —
new names are added by passing them to `run_universe.py` directly.

| Business model | Count |
|----------------|-------|
| `saas` | 78 |
| `semi_hardware` | 75 |
| `consumer_internet` | 42 |
| `deep_tech` | 18 |
| `hyperscale` | 8 |

**2026-08-08 batch — strictly technology.** 483 candidate tickers were pre-swept
with one AV `OVERVIEW` call each; only names AV tags `Sector=TECHNOLOGY` (plus
four verified successor symbols) were classified, because the 5-category engine
has no "not applicable" verdict and would force-fit banks/utilities/retailers
into a tech cell, polluting the peer-comparison tables. Skip record with reasons:
`universe_skipped_2026-08.tsv`; accept list: `universe_batch_2026-08.txt`.
Successor substitutions: `XYZ`←SQ, `PENG`←SGH, `GTM`←ZI, `VYX`←NCR
(`BYON`←OSTK dropped — Beyond is retail).

**Post-batch overrides (19).** The LLM tiebreaker again dumped fabless/analog
semis into `deep_tech` and high-margin vertical SaaS into `consumer_internet`;
all 19 were moved to their correct cell in `MANUAL_BM_OVERRIDES` (CSCO/SLAB/SYNA/
VYX → semi_hardware; MANH/TYL/FIVN/ALKT/MQ/DOX/RBRK/NICE/PAYX/RAMP/ROP/RPD/SPSC/
IT → saas; XYZ → consumer_internet).

**Taxonomy gap — IT-services / no-clean-fit (left LOW, not forced).** Labor-based
IT-services firms (CTSH, EPAM, GLOB, CNXC, EXLS, KD, SAIC, UIS, ACN, BR) and a
few others (CORZ bitcoin/HPC data centre, VSAT satcom, XPER media-IP, INOD AI
data-prep, WEX payments) have no correct home among the five categories. They
were deliberately **not** overridden — forcing them into `saas` would corrupt the
SaaS peer group with ~30%-margin businesses. A sixth "IT services / other"
category is the real fix and is deferred with the hyperscale split.

**Runtime exclusions from the 2026-08 batch:**
- *Staleness gate (>400d since last filing — acquired/delisted, AV still serves
  stale statements):* ANSS (Synopsys, 2025), EVBG, SPLK (Cisco, 2024), WKME.
- *Non-USD reporting currency:* ASML, IMOS, KSPI, PAGS, SAP, SRAD, STNE, TSM, UMC.
- *No AV coverage (53) / ETFs (8):* full list in the skip TSV.

**Non-USD exclusions** (yfinance `financialCurrency` gate): TSM (TWD),
ASML (EUR), SPOT (EUR), BABA (CNY). Price data stored, metrics skipped.

**Known dead symbols** (AV + yfinance both return nothing — do not re-add):
JNPR (acquired by HPE), ZI (reticked to GTM), PSTG, CFLT (taken private 2026).

**Empty matrix cells** — no ticker occupies hyperscale-4, saas-1, or deep_tech-4;
`semi_hardware-1` now has one (was empty). Those fair ranges remain untested
against real data — worth populating before the backtest is treated as complete.

**Interpreter note:** `/usr/bin/python3` is the only interpreter with the full
dependency set (yfinance + google.generativeai + supabase). Plain `python3`
resolves to 3.14 and lacks yfinance; the batch runner must be invoked as
`/usr/bin/python3 run_universe.py …`.

**Data-quality watch items:**
- MRVL cycle P/E = 3061x, inflated by `net_income_quality_flag` (NI includes a
  $2.67B non-cash gain vs OpInc*(1-tax) = $1.05B).
- INTC cycle P/E = 784x, AKAM CapEx EV/EBIT = 273x — both real but extreme;
  the verdict correctly reads Overvalued, but they distort any cell median.

---

## Web Application (Contextual Valuation Engine)

`Valuora_Previous_UI/app.py` — Streamlit, deployed on Streamlit Cloud.

**Auth (Supabase, email + password only — no OAuth).** Login is **optional**:
anyone can browse anonymously. Logged out, a `👤 Log in / Sign up` button sits
top-left with the signup pitch under it; watchlist features show prompts instead
of breaking. Session lives in `st.session_state.user`; the auth page is reached
via the `auth_view` session flag, and both login and signup offer
"← Continue browsing without an account".

**Navigation — five items:** `Home · Analysis · Watchlist · About · Other`.
Legacy pages (Financial Analysis, Company Profile, DCF Model, Macro Stress Test)
live under **Other** with an auto-fetching live-data gate. Right side of the nav
carries the 🔖 watchlist-count badge, display name, and Logout when signed in.

**Home** — (1) watchlist status board joining Supabase watchlist against SQLite
classifications, with an amber flag when a ticker's stage changed in the last
pipeline run; (2) classified-universe grid grouped by business model, pills
coloured by FH stage; (3) **Recently Moved** — `stage_change_log` entries from
the last 30 days, max 10.

**Analysis** — bookmark toggle + "alert me on stage change" at the top, then two
full-width rows (classification badges, then the four header KPIs), then a 40/60
split: matrix visualiser + FH scorecard on the left, CVE Interpretation +
valuation cards + peer comparison + Risk Flags on the right. Ticker selection is
a searchable `st.selectbox` over classified tickers (`"MSFT — Hyperscale · S3"`),
with a free-text expander for unclassified tickers that falls back to live data
only.

**Watchlist** — full KPI detail per ticker, primary metric, alert indicator,
per-row Remove, and a pipeline-last-run timestamp.

**Rendering note:** `st.dataframe` renders to an HTML5 canvas that **ignores page
CSS**, so it always looks light-themed. All themed tables go through the
`render_dark_table(headers, rows, highlight_first_col=None)` helper, which emits
a real HTML `<table>`. Don't reach for `st.dataframe` on a themed surface.

**Verdicts** are derived at *display* time by `derive_verdict(matrix_cell,
val_data)` from `FAIR_RANGES_FULL` — they are not stored. Changing a fair range
therefore takes effect immediately with no recompute.

---

## Stage-Change Detection & Alerts

`run_classification()` snapshots the previous `fh_stage` **before** the
`INSERT OR REPLACE`, and on a change:
1. appends to `STAGE_CHANGES_THIS_RUN` (module-level, for run reporting),
2. writes a row to Supabase `stage_change_log`,
3. calls `send_stage_change_alerts()`.

All of it is `try/except` wrapped — an offline run or unconfigured Supabase never
breaks the pipeline. `_get_supabase_client_module()` imports `supabase_client`
with a path fallback into `Valuora_Previous_UI/`.

**Alert emails** — plain text via `smtplib` (Gmail SMTP-SSL, port 465). Queries
Supabase for users with that ticker in their watchlist and
`alert_on_stage_change = TRUE`. Credentials come from `ALERT_EMAIL` /
`ALERT_EMAIL_PASSWORD` (a 16-character Google **App Password**, not the account
password). **Missing or bad credentials → DRY-RUN**: the batch logs
`[DRY-RUN] Would email …` and continues, rather than raising. Verified end to end
with a real send on 2026-07-16.

Emails are sent **by the pipeline running locally**, so the credentials must be
in `~/.zshrc` — putting them only in Streamlit Cloud secrets is not enough.

---

## Backtest Universe (`universe` table) — built 2026-08-11

Built by `universe_builder.py` from Alpha Vantage `LISTING_STATUS` CSVs
(`listing_status_active.csv`, `listing_status_delisted.csv`) plus a full
`OVERVIEW` crawl. **Separate from `classifications`** — `universe` is the
candidate pool for backtesting; `classifications` is what the engine has
actually classified (222 tickers).

| | |
|---|--:|
| rows | 11,009 (6,517 active / 4,492 delisted) |
| `in_backtest = 1` | **502** (488 active + 14 delisted) |
| `sector_unknown = 1` | 5,244 |

**Filters:** `assetType == Stock`; exchange ∈ {NASDAQ, NYSE}; delisted cut at
≥ 2012-01-01; **3,680 non-operating securities excluded** (2,932 units /
warrants / rights + 1,860 SPAC shells). The exclusion removes only the
derivative class, never the underlying — `GCTSW`/`GCTS-WS` go, `GCTS` stays.

**`in_backtest` rule:** sector is Technology (or a tech-adjacent *industry*)
AND market cap > $1B, OR the ticker is already in `classifications`.

**The 14 delisted names** (human-reviewed, `stage3_review.xlsx`): SPLK, LN,
INFA, OS, EVBG, WKME, ETWO, ENFN, MGIC, MIXT, STEC, SNCR, PET, EPWK.
Five are foreign issuers (LN, MGIC, MIXT, STEC, EPWK) and will likely be
dropped by the existing non-USD guard at classification, leaving ~9.

**Survivorship-bias reality check.** All 14 delisted 2024+ except LN (2020).
AV does not retain fundamentals for older delistings: of 362 delisted
tech-sounding candidates, **360 (99%) had no financial statements at all** and
the 2 survivors were a preferred share and a bond. This is an AV data limit,
not a filter problem — the delisted cohort can only speak to recent years.

**Known caveat — the market-cap floor is biased for delisted names.** Companies
usually shrink before delisting, so a $1B floor on *final* market cap keeps
premium acquisitions (winners) and drops failures — the opposite of what
survivorship-bias correction needs. 16 delisted tech names sat below the floor;
6 were admitted by manual review. Consider peak (not final) market cap if this
is revisited.

---

## AV Data Defects — read before trusting Alpha Vantage output

Hard-won during the 2026-08 universe build. Every one of these silently
produces plausible-looking wrong answers.

**1. AV returns the literal string `"None"`, not JSON null.**
`(ov.get("Sector") or "").strip() or None` lets `"None"` through as if it were
a real value. Use `_av_str()` (in `universe_builder.py`). This silently dropped
~6% of tickers from review lists.

**2. AV freezes delisted tickers — it does not stop serving them.**
After the last real trade the close is carried forward with zero volume,
forever:
```
SPLK 2024-03-15 close=156.90 vol=18,211,874   <- last real day
SPLK 2024-03-18 close=156.90 vol=0            <- Cisco deal closes
SPLK 2026-08-10 close=156.90 vol=0            <- still "trading" 2.4y later
```
One distinct close across 2.4 years. **A backtest reading this holds a flat,
riskless, never-exiting position in a dead company.** This is why
`BACKTEST_RULES.md` Rule 1 exists. Detector: last bar with volume ≥ 1% of the
ticker's median volume (validated within one trading day on SPLK, SGEN, ACIA,
CLDR, ARUN, EVBG).

**3. `LISTING_STATUS` delisting dates are sometimes the file's snapshot date.**
597 of 9,410 rows carried 2026-08-07 instead of the true date (Splunk, Seagen
included). Repaired by `fix_delisting_dates.py`. `universe.delisting_date`
stores the **last tradable day**, deliberately *not* AV's convention (the
following business day), so it can be used as a direct trading cutoff.

**4. AV discards fundamentals for older delisted tickers** (but keeps prices).
Brocade, VASCO, Catalyst Semiconductor, Cloudera, Coupa: zero statements.
Check statement availability *before* spending human review time on a name.

**5. Ticker symbols get reused.** `SOS` = Storage Computer Corp (delisted) →
SOS Ltd (live). Also `BMM`, `PALX`, `RAC`, `SSM`. Splicing a reused symbol
joins two different companies into one price history. 32 such rows are
`in_backtest = 0` with `delisting_date = NULL`.

**6. AV's delisted CSV contains live companies.** `SMTK` and `ASNS` were listed
as delisted while trading millions of shares and filing current financials;
both were corrected to `is_delisted = 0`.

**7. yfinance cannot substitute for delisted data** — it returns ZERO bars for
delisted symbols (verified: SPLK, SGEN, ACIA, CLDR, ARUN). Yahoo purges them.

**8. Substring matching on industry strings is a trap.**
`"BIOTECHNOLOGY"` contains `"TECHNOLOGY"` — this swept 179 biotechs into the
backtest universe (25% of it). But a full `\b…\b` match is ALSO wrong: it
fails on `SEMICONDUCTORS` (AV's plural industry string) and would drop every
chip company. Correct rule = **leading word boundary only**.

**9. AV throws spurious `Invalid API call` errors** for valid symbols under
sustained load; they succeed on retry. `_av_fetch` treats these as hard errors
and does NOT retry — wrap it (see `_av_fetch_retry` in
`backfill_fundamentals.py`).

---

## Conventions

- All metrics on LTM (last 12 months) basis except balance sheet items (MRQ).
- SBC adjustment applied to FCF BEFORE FCF margin threshold is checked.
- FCF margin adj = (FCF - SBC) / Revenue.
- Cyclical correction: semis use `cycle_adj_pe` (5-yr avg NI), never trailing P/E.
- Standalone modules (metrics_extractor.py) read/write the same SQLite DB.
- `data_pipeline1.py` owns the schema (setup_database). Other files never ALTER.
- Log every LLM call and fallback path for audit trail.
- `as_of_date` = date of most recent fiscal quarter end used in computation.

---

## Backtest #1 — Progress & Results (steps 1–4 complete, 2026-08-11)

Spec: `~/Downloads/CVE_Backtest_01_Plan.md`. Steps 5–7 remain (Test 2,
portfolio sim, Streamlit page).

### Classification expansion
222 → **457 tickers**. `run_universe.py` classified 228 of the 273 candidates;
**38 were excluded by the B6 currency gate** (ASML/EUR, BABA·BIDU·BILI·BZ·ATHM/CNY,
ASX/TWD, ERIC/SEK and others — the gate working as designed), the rest failed on
thin data. BM mix now: semi_hardware 152, saas 128, consumer_internet 109,
deep_tech 53, hyperscale 8.

**`run_universe.py` cannot classify delisted names** — it calls
`fetch_and_store_price_data` (yfinance) first, which returns zero bars for
delisted symbols, and aborts before classifying. That silently dropped 8 of the
14 delisted names even though their AV-backfilled prices were already in
`price_data`. Fixed by **`classify_delisted_offline.py`**, which runs the
engine's own `classify_business_model` / `classify_financial_health` against
locally-computed metrics with no network price fetch. 13 of 14 delisted names
are now classified (STEC fails — no CASH_FLOW statements, it is in
`backtest_data_gaps.tsv`).
- *Known divergence:* yfinance also supplies `deferred_revenue` for the SaaS
  gate. Offline it is NULL, so the gate demotes a passing SaaS validator. With
  the Gemini tiebreaker available the demotion resolves back to `saas`
  (SPLK/WKME → saas-3). **The script MUST be run with the zshrc-loaded API key**
  or the LLM is unavailable and SPLK/WKME land in `consumer_internet` instead.
- EVBG → consumer_internet-2 and MIXT → consumer_internet-3 are the engine's own
  tiebreaker outputs and are debatable (both look like SaaS). Left as the engine
  produced them rather than hand-tuned. The LLM tiebreaker is non-deterministic:
  ENFN came out `saas-2` on one run and `consumer_internet-2` on the next.

### Stage 1 — `backtest_panel` (`build_panel.py`)
**17,366 rows · 432 tickers · 2012-01-01 → 2026-07-01**, quarterly grid (59
as-of dates). 95% carry a verdict; 15,706 have a 12-month forward return.
Integrity assertions both return **0**: no row uses a filing dated after its
as-of date, and no row exists at or after a ticker's delisting date. Ticker
count grows 190 (2012) → 423 (2026) as companies IPO, which is B2 working.
12 delisted names contribute 292 rows, 45 of them delisted-in-period.

Part B compliance: B1 filing-date gate via
`COALESCE(reported_date, fiscal_date+45d)`; B2 IPO+4q and delisting gates;
B3 NTM proxy on 7,000 rows (all flagged `ntm_proxy_used=1`); B4 adjusted close
throughout; B5 force-exit at last real close (Rule 1); B6 non-USD filers never
enter `classifications`.

**⚠ Panel does NOT use `app.py`'s verdict plumbing — deliberate, see next.**

### Defect found: 7 of 20 cells render no verdict in the live app
`FAIR_RANGES_FULL` (`app.py`) points every **"EV/NTM Rev"** cell at the key
`ev_ntm_arr`, but `data_pipeline1` writes that key only when the routed method
is `"EV/NTM ARR"`. For `semi_hardware-2` (22 tickers), `consumer_internet-2` (9)
and `deep_tech-2` (6) the pipeline writes **`ev_ntm_revenue`**, so the lookup
misses and `derive_verdict()` returns `None`. Separately the three **S1** cells
(`hyperscale-1`, `semi_hardware-1`, `consumer_internet-1`) route to
`"EV/Revenue and CapEx/TTM Revenue"` / `"P/S and MAU"`, which compute neither
key. Verified against `valuations`: `semi_hardware-2` has 22/22 rows with
`ev_ntm_revenue` and 0 with `ev_ntm_arr`.

`build_panel.py` therefore carries its own `PANEL_MATRIX` and computes each
cell's documented primary metric directly. Bands match the v2 matrix table above
exactly. **This bug is still live in the app and is not yet fixed.**

### Stage 2 — Test 1 results (`analyze_test1.py`)
**The hypothesis holds, moderately, and mostly on the median.**

Pooled 12m: undervalued mean +29.7% / median +15.8% / hit 67.3% (n=7,764) vs
overvalued mean +23.3% / median +8.5% / hit 57.1% (n=3,236) → spread **+6.4pp
mean, +7.3pp median**. Signal strengthens with horizon: the 3m spread is only
+0.2pp mean, the 6m +1.2pp.

Within-cell, 11 cells clear 30 stock-quarters on both legs:
**10 of 11 confirm on median spread; 8 of 11 on mean.**

| Cell | median pp | mean pp | n(u)/n(o) |
|---|--:|--:|---|
| deep_tech-3 | +54.3 | −156.4 | 196/34 |
| deep_tech-2 | +37.8 | −17.4 | 291/84 |
| saas-2 | +32.9 | +8.2 | 982/109 |
| consumer_internet-2 | +27.9 | +35.0 | 980/113 |
| saas-3 | +9.9 | +6.4 | 825/638 |
| semi_hardware-4 | +8.8 | +4.5 | 141/69 |
| semi_hardware-2 | +6.4 | +7.9 | 1624/287 |
| semi_hardware-3 | +5.4 | +1.7 | 983/1255 |
| consumer_internet-3 | +4.3 | +2.8 | 1087/325 |
| saas-4 | +1.6 | +1.9 | 264/96 |
| hyperscale-3 | −2.1 | −1.4 | 72/76 |

**Use the median.** Forward equity returns are violently right-skewed — one
10-bagger in a 34-name leg swings the mean by hundreds of points.
`deep_tech-3` is the clearest case: overvalued mean +183.7% with sd **790** but
median **−41.0%**, so the mean spread reads −156pp while the median reads +54pp.
The mean is the outlier artifact; the median is the signal.

**Regime split (pass condition 3 — satisfied, 2 of 3):**

| Regime | n(u)/n(o) | mean u% | mean o% | spread |
|---|---|--:|--:|--:|
| ZIRP 2012–2021 | 5143/1866 | 29.4 | 17.9 | **+11.5** |
| Rate shock 2022–23 | 1083/461 | 22.5 | 2.5 | **+20.0** |
| AI cycle 2023–26 | 1538/909 | 35.6 | 44.8 | **−9.2** |

The engine is strongest when rates bite and **inverts during the AI cycle** —
since mid-2023 "overvalued" names outran "undervalued" ones. Momentum beating
value in a melt-up is a real finding, not a bug, and it is the single biggest
threat to the Stage 4 portfolio result.

**Verdict calibration is skewed cheap:** 8,428 undervalued vs 3,772 overvalued
across the panel, and 75.6% of 2012 rows read undervalued vs 43.1% in 2026. The
bands are absolute, so the decade-long multiple expansion drags the whole
universe from "cheap" to "expensive". Regime-conditioning the ranges is the
obvious follow-up (already an Open Item).

**Pass conditions so far:** #1 met (10/11 cells, samples ≫30) · #3 met (2 of 3
regimes) · #4 met (signal in 10 cells, not 1–2) · #2 untested until Stage 4.

### Stage 3 — Test 2 results (`analyze_test2.py`) — **the claim FAILS**

**The S2→S3 transition does not predict returns.** This was billed as the
strongest single claim in the framework; the panel does not support it.

1,075 S2→S3 events across 343 tickers and 58 quarters. Raw forward returns look
healthy — **+12.2% median 12m, 62% hit rate** — but that is market beta, not
signal. Every event is differenced against its **own quarter's** control median
(same-cell non-transitioning peers, and the full eligible universe), because
2021 was a good year for everything:

| Measure (12m) | n | mean pp | median pp | hit% |
|---|--:|--:|--:|--:|
| raw return | 975 | +27.2 | +12.2 | 62.1 |
| excess vs same-cell peers | 975 | +11.7 | **−0.6** | 49.2 |
| excess vs universe | 975 | +13.1 | **−1.0** | 48.9 |

Median excess is ~0 and the hit rate is a coin flip. As in Test 1 the mean is a
right-skew artifact (sd 77) — believe the median.

**The mirror case kills it.** S3→S2 — a *falling* stage, which Stage 4's sell
rule fires on — produces 984 events with **+15.1% raw median and +0.3pp excess
vs cell**. A stage fall is indistinguishable from a stage rise. If both
directions pay the same, the variable carries no information.

**Boundary oscillation is most of the event set, but filtering it does not
rescue the claim.** `fh_stage` is a rounded weighted average, so names sitting
on the 2/3 line flip every quarter (1,075 rises vs 984 falls is that signature).
Filtering to *durable* (stage held the next quarter) and *first-time* (never
reached S3 before) crossings moves the number the right way but not far enough:

| Event set | n | raw med% | excess vs cell |
|---|--:|--:|--:|
| all 2→3 | 975 | 12.2 | −0.6 |
| durable | 766 | 11.6 | −0.1 |
| first-time crossing | 163 | 14.3 | +0.9 |
| **durable AND first-time** | 131 | 13.4 | **+1.8** |
| oscillation (neither) | 177 | 14.8 | −2.4 |

The cleanest possible subset — a first, sticky crossing into FCF-positive —
earns **+1.8pp median over its cell peers at a 52.7% hit rate**. Directionally
right, economically trivial, and 0.5% round-trip costs eat most of it.

By BM: only `semi_hardware` (+0.9, n=493) and `deep_tech` (+1.3, n=47) are
positive; `consumer_internet` (−2.8, n=246) and `saas` (−2.4, n=177) are
negative. `hyperscale` shows +9.0 on n=12 — insufficient, ignore it. By regime
the AI cycle is the only positive one (+3.3, n=247) — the reverse of Test 1,
where the AI cycle was the regime that inverted.

**Interaction with the verdict (this is the useful finding).** Transitions that
happen while the name reads *undervalued* (+1.6) or *fair* (+1.8) beat cell
peers; those reading *overvalued* (−1.7) or carrying no metric (−6.2) lose. The
verdict is doing the work, not the transition. **Stage 4 should therefore buy on
verdict alone and not add a transition trigger** — and the sell-on-falling-stage
rule is not supported by evidence either, so its cost must be measured rather
than assumed (see the sell-rule variant in Stage 4).

### Stage 4 — portfolio simulation (`portfolio_sim.py`) — **pass condition 2 FAILS**

£10,000 notional · 0.5%/trade · 35% cap · quarterly rebalance on the panel's
filing-gated grid · 2012-01-01 → 2026-08-01.

| Run | Final £ | CAGR% | maxDD% | Sharpe | trades |
|---|--:|--:|--:|--:|--:|
| **5 positions (headline)** | **77,231** | 15.0 | −38.6 | 0.69 | 190 |
| 10 positions | 117,465 | 18.4 | −29.6 | 0.86 | 338 |
| 20 positions | 99,290 | 17.0 | −33.5 | 0.84 | 518 |
| SPY | 76,616 | 14.9 | | | |
| QQQ | 139,517 | 19.8 | | | |
| **Equal-weight CVE universe** | **239,366** | 24.5 | | | |

**The strategy loses to both required benchmarks at every concentration.** It
edges SPY (£77.2k vs £76.6k — inside noise) and is beaten by QQQ by 45% and by
its own equal-weight universe by 69%. Buying every eligible name in the CVE
universe and rebalancing quarterly beat picking the "undervalued" ones by 3.2x.

**Concentration sensitivity (required): 10 > 20 > 5.** The answer to "is this
concentration luck?" is the reverse of the one the plan anticipated —
concentration *hurts*. 5 positions is the worst of the three.

**The selection ranking is actively harmful.** "Buy undervalued" leaves ~140
candidates for 5 slots, so a ranking had to be invented: depth below the cell's
fair band, normalised by band width. Picking at *random* from the same pool
returns a median **£143,759 over 20 seeds** — 1.9x the depth-ranked run, and the
only configuration that edges QQQ (+3.0%). Depth-ranking loads up on
extreme-yield S4 names, which behave like value traps. The random spread
(£40k / £144k / £372k min/median/max) also shows luck dominates at 5 positions:
a 9x range from seed choice alone.

**The sell rules cost something, but less than first measured.** Dropping
sell-on-falling-stage — which Test 2 showed carries no information — returns
**£93,162 (Sharpe 0.87 vs 0.69) on 21 trades**, better than the spec rules but
**still losing to QQQ by 33%**. It degenerates toward buy-and-hold of a handful
of names, so treat it as evidence against the stage-fall rule rather than as a
strategy. Turnover on the spec rules is **114%/yr**, well past the 60% level at
which Part D says costs dominate.

> **Correction (same session).** This variant was first recorded at £167,125
> "beats QQQ +19.8%". That number was an artefact of a corrupted KLAC price
> series (see *Price integrity defect* below) which manufactured a +10,295%
> round trip. After the repair it is £93,162 and beats nothing. The headline
> 5-position run was unaffected — it never held KLAC across the break.

**Only 5 of 20 cells ever fire** — saas-4 (39 buys), semi_hardware-4 (33),
consumer_internet-3 (20), hyperscale-4 (3), saas-3 (2). Pass condition 4 holds
on the panel (Test 1, 10 cells) but not in the portfolio, because the depth
ranking funnels everything into FCF-yield cells.

**Regime:** beats QQQ only in the rate shock (+13.1pp on a flat 2.4% vs −10.7%);
loses ZIRP by 416pp and the AI cycle by 3pp — so **pass condition 3 fails at the
portfolio level** (1 of 3 regimes) even though it held for Test 1. Same shape as Test 1 — the engine
is a defensive-value signal that works when rates bite.

**D3 survivorship bound is VACUOUS and must not be quoted as a pass.** Forcing
delisted holdings to −50% / −100% changes nothing because the strategy **never
bought a delisted name** (12 delisted tickers, 292 of 17,366 panel rows, never
top-5 on depth). The bias is unbounded by that test, not bounded by it.

**Rule 1 mechanism proof (closes the open item in `BACKTEST_RULES.md`).**
Identical D3 results are ambiguous between "the exit works and cost nothing" and
"the exit never fired", so a run with the candidate pool **restricted to
delisted names** forces the path: **8 forced exits fire** (LN, MIXT, WKME, PET
@ $0.05, ETWO…), each at the last real close on the true final trading day, and
the terminal-loss override moves the result hard — £27,967 base → £9,097 at
−50% → £1,893 at −100%. The hard-stop reaches the portfolio loop.

**Methodology notes (all disclosed in the Part E limitations panel):**
- Execution is at the **next close after** the signal date, not the close on or
  before it. The filing gate admits statements dated up to the rebalance date,
  so trading on that session's close was a look-ahead; removing it cost ~4%
  (£80.5k → £77.2k) and changed no conclusion.
- "Ticker exits cell" is implemented as *re-evaluate in the new cell, sell only
  if now overvalued*. Read literally the rule fires on any stage change —
  including an upgrade — because `matrix_cell` is `bm-stage`, which would sell
  every winner the quarter it improves.
- Cash is deployed across the slots being filled rather than sized at
  pv/positions, so cash drag (1.0%) measures quarters where nothing qualified
  rather than a sizing artefact.
- Round trips are attributed to the cell they were **bought** for; a holding's
  live cell moves with its stage.
- The engine's taxonomy gaps show up directly in the trade log — CME, PAYX and
  IDCC traded as `saas`, DXC/UIS/UPBD as `consumer_internet`. These are the
  IT-services / no-clean-fit names already flagged in *Known Open Items*.

### Price integrity defect — splits corrupt stored history (FOUND AND FIXED)

**Found by reading the Part E trade log**, not by a test: KLAC showed a
**+10,295% round trip**. KLA has never returned 100x in nine years.

**The defect.** `price_data` stores *back-adjusted* prices (yfinance
`auto_adjust=True`) and writes them with `INSERT OR IGNORE`. Back-adjustment is
anchored to the present, so **when a ticker splits, the correct adjusted price
for every earlier date changes by the split factor**. Rows already stored are
never revisited, so the series ends up in two pieces on different scales, joined
wherever the next ingest happened to start:

```
KLAC 2021-05-19  close=  28.89     <- rows written post-split, correctly adjusted
KLAC 2021-05-20  close= 297.87     <- rows written pre-split, 10x scale  (KLA split 10:1)
KLAC 2026-05-21  close= 184.22     <- back to the correct scale
```

A uniform factor across a whole series is harmless — returns are ratios, so a
constant cancels (CRWD was uniformly 4x for most of its history and its returns
were fine). **Only a break *within* a series corrupts returns**, and it does so
catastrophically: a 10:1 split reads as a +931% one-day gain.

**Scope.** `price_integrity_check.py` compares every panel ticker against a
fresh yfinance pull: **408 clean, 2 genuinely broken (KLAC 10x, CRWD 4x), 12
last-bar noise** (2–7% on the final bar — stored close vs live quote, not
corruption), 12 unchecked (delisted names come from AV and have no yfinance
presence, so this test cannot see them).

**Fixed.** `price_integrity_check.py --fix` re-pulls anything above a 1.10
spread on one consistent basis, full OHLCV. Panel, Test 1, Test 2 and every
portfolio run were rebuilt on the corrected prices.

**What changed after the repair:** Test 1 conclusions unchanged (still 10 of 11
cells confirming). Test 2 unchanged. The **5-position headline was unaffected**
(it sold KLAC after one quarter, before the break). The **no-sell variant
collapsed from £167,125 to £93,162** and no longer beats QQQ — that finding had
been entirely manufactured by the bad data.

**This will recur.** Every future split re-creates it for any ticker whose rows
predate the split. `price_integrity_check.py` should run after each backfill,
and the real fix is for the ingest path to re-write (not `INSERT OR IGNORE`)
history when a split is detected.

### `integrity_check.py` — standing data-integrity suite (built 2026-08-15)

Built to `~/Downloads/integrity_check_spec.md`. 33 tests across 5 suites, written
*independently* of the code that produced the data: they ask "is this value
physically possible", not "did the pipeline run". **Reports only — never repairs.**
Exit 1 on any CRITICAL, so it can gate the pipeline.

```bash
caffeinate -i /usr/bin/python3 integrity_check.py --all 2>&1 | tee integrity_run.log
/usr/bin/python3 integrity_check.py --ticker NVDA     # one ticker, all suites
/usr/bin/python3 integrity_check.py --report          # reprint last run
```
Results checkpoint to `integrity_results` per suite; detail to
`integrity_failures.csv` (never to console).

**P2 confirms the KLA repair holds — 0 scaled segments.** The detector that
would have caught the original defect now finds nothing, independently of
`price_integrity_check.py`.

**Four spec tests were not runnable as written** and say so in the output rather
than silently passing: P2/P4 need a raw close alongside the adjusted close
(`price_data` stores only the adjusted one — **storing raw close is the single
highest-value schema change**, it would catch splices that produce no visible
jump); F7 has no `period_type` column; R5's `valuation_engine.py` does not exist,
so it reads `FAIR_RANGES_FULL` out of `app.py` by AST instead.

**8 CRITICAL failures. None invalidate Backtest #1 — combined contamination is
under 0.5% of panel rows — but three need fixing before Backtest #2:**

| Test | Finding | Study impact |
|---|---|---|
| **T1** | **554 rows with `reported_date <= fiscal_date`** — a quarter cannot be filed before it ends. 471 on panel tickers (APLD 65, APPS 40, SHAZ 32, RZLV 28, OLED 24, CRWV 16). | **The most consequential.** A filing dated at its own quarter-end enters the panel ~30–45 days before it was public — a real point-in-time leak. **Fix: treat `reported_date <= fiscal_date` as missing and fall back to `fiscal_date + 45d`.** |
| **F5** | **3 tickers change reporting currency mid-series** — NBIS (RUB:173 / USD:22), MIXT (ZAR:145 / USD:3), LN (USD:50 / JPY:15). All three are in the panel (58 rows). | The B6 currency gate reads yfinance's *current* `financialCurrency` and is blind to a mid-series change. NBIS is 1 of only 8 hyperscale names and carries a manual override, so its ratios matter out of proportion to its row count. |
| **P1** | 44 day-over-day moves at a round split ratio; 33 on panel tickers since 2011. | **21 panel rows** (0.12%) have a 12m window spanning one — APPS, ASNS, BKSY, ETWO, DLO, APLD, KEEL, LUNR. Same class as the KLA defect, in small caps. Needs per-name review; too small to move any conclusion. |
| F1 | 127 balance sheets off by >25% (assets ≠ liabilities + equity) | Parse failures; affects debt/equity and EV inputs. A further 2,102 off by 1–25% are reported separately as F1b — that band is normally non-controlling interests, which AV excludes from `totalShareholderEquity`, not a bug. |
| F4 | 44 order-of-magnitude revenue changes | Mostly pre-2012 micro-caps outside the window (APLD 2002). |
| P3 | 5 NULL closes, all on 2026-06-26 | One bad ingest day. Trivial. |
| R5 | **10 of 19 sampled pairs disagree** between the app and the panel | Quantifies the known `ev_ntm_arr` bug — the app renders *no verdict* for the affected cells. Independent confirmation. |
| X1 | 1 row (ASNS) | Was 318 until the test was corrected — see below. |

**Two of my own test bugs, found and fixed during the build** (worth recording,
because both are the kind that make a check look clean when it isn't):
- **SQL precedence.** `WHERE a > 1000 OR b < -99 AND ticker=?` binds as
  `a OR (b AND ticker)`, so `--ticker NVDA` silently returned the whole
  universe. Parenthesise the OR group before appending a filter.
- **`LIMIT` reported as a count.** "5,000 rows before ipo_date" was the limit,
  not the finding. Count and sample must be separate queries.
- X1 originally compared `computed_metrics.market_cap` against the price at
  `as_of_date` and flagged 318 tickers. `market_cap` is built from the **live**
  price at pipeline-run time (ratio against the latest close is exactly 1.000
  across the table), so the test was measuring elapsed market movement. Now
  compared on its own terms, with the misalignment reported as X1b. Worth
  knowing: `computed_metrics.market_cap` is *not* point-in-time with its own
  `as_of_date`. `backtest_panel` recomputes market cap itself and is unaffected.

### Backtest 1.1 (RR) — Phase 1 cleaning + Phase 2 verdict unification

DB backed up first: `valoura_backtest.db.bak.20260815_173408`.
**CRITICAL integrity failures 8 → 0** (`integrity_check.py --all` exits 0).

#### Phase 1 — exclusions and repairs (`phase1_clean.py`, logged to `data_exclusions`)

| Defect | Excluded | Flagged | Note |
|---|--:|--:|---|
| T1 | 554 rows | — | `reported_date=NULL`, `reported_date_missing=1`; panel drops them |
| F5 | **3 tickers** | — | LN, MIXT, NBIS |
| F1 | 37 rows | 125 rows | 2 sign-flips + 35 genuine breaches |
| F4 | 4 rows | 64 rows | units errors out, ramps reviewed |
| P3 | 5 rows | — | one bad provider day |
| X1 | **1 ticker** | — | ASNS (stale share count) |
| P1 | 0 | 6 adjudicated | **no repair needed** |

**Tickers excluded entirely: LN, MIXT, NBIS, ASNS.** `in_backtest` 502 → 498.

**Three briefed fixes did not survive contact with the data:**
- **T1 was never our parser.** `backfill_fundamentals.py` already used
  `rmap.get(fd)` (None on a miss). **Alpha Vantage itself emits
  `reportedDate == fiscalDateEnding`** — confirmed against the *live* API, which
  still returns 17 such quarters for APLD, 16 for SHAZ, 7 for RZLV. **0 of 554
  recoverable**; the real dates do not exist at this provider. Gate added at
  ingest: `backfill_fundamentals.py::_valid_reported_date`. No lag imputed.
- **P1 found no surviving splits.** All 13 checkable tickers match a fresh
  yfinance pull (spread ≤1.10). 38 of 44 flagged moves sit on a volume spike vs
  their own 60-day median — ACLS 0.337 is the real Sept-2008 crash (10.9x
  volume), ASNS 6,135x, LUNR 163x. The round-ratio rule was the defect. Fixed
  with volume corroboration (44 → 6) plus an adjudication table so settled
  human decisions are not re-litigated each run. The split repair **is**
  direction-agnostic (it compares min/max ratio spread, catching reverse splits
  — KLAC's own fix included a 0.101 downward leg).
- **F1's sign flip explains 2 of 127**, not most. The other 125 are
  non-controlling interests AV excludes from `totalShareholderEquity`. Splitting
  on the sign of `A−L−E` isolated **35 genuine breaches**, which were excluded.

**P6 is 120,680 rows / 106 tickers, not 5,000 / 9** (that was a CSV row cap),
and **the instructed fix was deliberately not applied**: for 27 tickers with
`ipo_date` after 2011 (DXC, MTCH, QUBT, RZLV and other de-SPACs) the ipo_date is
*correct* and the earlier prices belong to the shell or predecessor. Moving
ipo_date back would inject that history into the panel. The other 79 are
off-by-days (33) or pre-2012 legacy dates (46) that cannot affect a 2012+ panel.

#### Phase 2 — one verdict function (`valuation_engine.py`, NEW)

The rule lived in two places and drifted. It now lives **only** in
`valuation_engine.py`; `build_panel.py` and `app.py` both import it.
- `build_panel.py`: `PANEL_MATRIX` + `verdict_for()` **deleted**.
- `app.py`: local `FAIR_RANGES_FULL` **deleted**; `derive_verdict()` delegates
  to `ve.verdict()` and only title-cases for display.
- Metric lookup is by an **ordered key list per metric** with normalisation, so
  `ev_ntm_arr` / `ev_ntm_revenue` / `EV/NTM_ARR` all resolve.
- **`data_pipeline1.VALUATION_MATRIX` re-routed at source**: the three S1 cells
  and `saas-2` now compute `ev_ntm_revenue`. They previously routed to
  "EV/Revenue and CapEx/TTM Revenue" / "P/S and MAU", which compute neither key
  — the other half of the blank-verdict defect. 50 tickers recomputed.

**R5 verification: 50 sampled pairs · 0 blanks · 0 contradictions.** All 18
populated cells resolve (hyperscale-4 and saas-1 remain unoccupied). R5 now also
asserts *structurally* that app.py holds no second copy of the rule.

> **Watch item for Test 1 interpretation:** saas-2's band (8–18x) was calibrated
> for EV/NTM **ARR**, but ARR is known for almost no ticker, so the cell falls
> back to EV/NTM **Rev** against the same band. WB reads 1.39x and STUB 1.84x →
> both "undervalued". This is unchanged from Backtest #1 (the old PANEL_MATRIX
> did the same), but it means saas-2's large positive spread may be a band/metric
> scale mismatch rather than signal.

#### Phase 3 — Test 1 rerun as Backtest 1.1 (RR) (`analyze_test1_rr.py`)

Panel rebuilt from cleaned data: **17,291 rows · 427 tickers**, every row tagged
`backtest_version = '1.1-RR'`; `matrix_version` unchanged at `v2-2026-08`.

**Headline: the cleaning changed almost nothing. The bootstrap changed a lot.**

| Cell | before | after | change | n(u)/n(o) | 95% CI | verdict |
|---|--:|--:|--:|---|---|---|
| deep_tech-3 | +54.3 | **+53.8** | −0.5 | 196/33 | [+25.9, +65.4] | CONFIRMS |
| saas-2 | +37.0 | **+37.0** | −0.0 | 984/102 | [+17.7, +54.4] | CONFIRMS |
| deep_tech-2 | +37.8 | **+36.6** | −1.2 | 291/83 | [+15.8, +51.0] | CONFIRMS |
| consumer_internet-2 | +27.9 | **+27.4** | −0.6 | 956/112 | [+16.8, +38.8] | CONFIRMS |
| saas-3 | +9.9 | **+9.6** | −0.2 | 828/638 | [+5.5, +14.1] | CONFIRMS |
| semi_hardware-3 | +5.5 | **+5.5** | +0.0 | 983/1252 | [+1.5, +10.1] | CONFIRMS |
| semi_hardware-4 | +10.0 | +10.0 | +0.0 | 145/60 | [−5.9, +35.8] | not distinguishable |
| semi_hardware-2 | +6.4 | +6.5 | +0.1 | 1617/287 | [−2.8, +14.2] | not distinguishable |
| consumer_internet-3 | +4.3 | +4.5 | +0.2 | 1065/321 | [−3.1, +12.9] | not distinguishable |
| saas-4 | +1.6 | +2.2 | +0.5 | 261/96 | [−9.9, +21.4] | not distinguishable |
| hyperscale-3 | −2.1 | −2.1 | +0.0 | 72/76 | [−10.4, +8.0] | not distinguishable |

**Backtest #1 said "10 of 11 cells confirm". With confidence intervals, 6 of 11
survive and 5 are statistically indistinguishable from zero.** No cell
contradicts. That correction comes from adding uncertainty quantification, not
from cleaning — the original analysis simply had none.

**The contamination hypothesis is not supported.** The expectation was that
defects clustering in APLD / QUBT / LUNR / ASTS / ACHR would deflate the
deep-tech cells. Two independent checks say otherwise:
1. Every one of those tickers contributes **exactly the same number of panel
   rows before and after** (APLD 17→17, QUBT 28→28, LUNR 15→15, ASTS 23→23,
   ACHR 19→19). The T1 exclusions were mostly EARNINGS rows, which
   `compute_metrics` never reads, and the affected statement quarters had
   surviving neighbours.
2. **The deep-tech cells are not made of those names.** deep_tech-2's largest
   contributors are VRNS, AGYS, APPS, BAND, LDOS, CACI; deep_tech-3's are JKHY,
   ACIW, CACI, CEVA, LDOS. These are the profitable **IT-services / no-clean-fit**
   companies from the known taxonomy gap, not speculative pre-revenue deep tech.
   deep_tech-3's +53.8 is real and survives the bootstrap — but it is evidence
   about a mislabelled IT-services bucket, not about deep tech.

**Correction to a Backtest #1 headline.** The regime table there used the MEAN
and reported the AI cycle *inverting* (−9.2pp). On the median — the statistic
that analysis itself said to believe — no inversion exists:

| Regime | before | after | n(u)/n(o) |
|---|--:|--:|---|
| ZIRP 2012-21 | +5.6 | **+5.6** | 5111/1859 |
| Rate shock 2022-23 | +19.8 | **+19.9** | 1081/449 |
| AI cycle 2023-26 | +1.2 | **+1.2** | 1527/901 |

All three regimes are positive on the median; the engine is strongest when rates
bite and roughly flat in the melt-up. **Pass condition 3 holds on the median.**

Pooled: 12m spread +7.4pp median (u 15.9% vs o 8.5%, hit 67.4% vs 57.0%);
horizon monotonicity intact (3m +1.4 · 6m +2.8 · 12m +7.4).

Band drift persists and is the clearest remaining calibration problem:
**75.6% of 2012 rows read undervalued vs 43.0% of 2026 rows.**

> **Treat saas-2's +37.0 with suspicion despite its interval.** Its band (8–18x)
> was calibrated for EV/NTM ARR, but ARR is unknown for nearly every ticker, so
> the cell compares EV/NTM **Rev** against an ARR band — WB at 1.39x and STUB at
> 1.84x both read "undervalued". Same behaviour as Backtest #1, so the
> comparison is valid, but the level is probably a scale mismatch.

### Band calibration — four schemes tested (`analyze_bands.py`, 2026-08-15)

**Why:** the absolute v2 bands drift (75.6% undervalued in 2012 → 43.0% in 2026),
producing legs of 984/102 (saas-2), 956/112 (consumer_internet-2), 1617/287
(semi_hardware-2), 196/33 (deep_tech-3). Those are not cheap-vs-expensive
comparisons; they are the bulk of a bucket against a small extreme tail.

Schemes, all on the same 1.1-RR panel and the same forward returns:

| | scheme | drift | live-usable |
|---|---|---|---|
| A | **absolute** — current v2 bands | 75.6% → 43.0% | yes |
| B | **percentile** — cheapest vs dearest third within cell AND quarter | **31.9% → 32.4%, flat by construction** | yes |
| C | **regime** — bands refitted per rate era (33rd/67th pct) | 56.3% → 34.4% | **no — hindsight** |
| D | **rolling** — 0.7x–1.3x the cell's trailing 5y median, point-in-time | 27.5% → 36.1% | yes |

#### The headline: the lopsided legs were carrying the result

| Cell | absolute | percentile | regime | rolling |
|---|--:|--:|--:|--:|
| saas-2 | **+37.0** CONFIRMS | **−15.0 CONTRADICTS** | +11.8 CONFIRMS | −9.4 n.d. |
| consumer_internet-2 | **+27.4** CONFIRMS | **−10.3 CONTRADICTS** | −6.5 n.d. | −8.7 CONTRADICTS |
| deep_tech-3 | **+53.8** CONFIRMS | +22.6 n.d. | +14.0 CONFIRMS | +6.6 n.d. |
| semi_hardware-2 | +6.5 n.d. | +1.2 n.d. | +6.5 CONFIRMS | +7.2 CONFIRMS |

Balance goes 0.10 → 1.00 under the percentile split, and **saas-2 and
consumer_internet-2 change sign**. The two largest "confirming" spreads in
Backtest 1.1 were artefacts of tail-vs-bulk comparison.

**Under a strict cross-sectional test (percentile), NOT ONE CELL CONFIRMS.**

#### What that means

Absolute bands conflate **market timing** with **stock selection**. Held fixed
across 2012–2026 they mostly label whole *eras* cheap or expensive (everything
is cheap in 2012), so the "spread" is substantially "buy when the whole cell is
cheap", not "buy the cheapest names in the cell". Rank within a quarter — which
removes the time dimension entirely — and the cross-sectional signal is ~0.

The schemes that keep a signal (regime, rolling) both retain a time-series
component, which is consistent with that reading rather than contradicting it.

#### Pooled by business model (stages 2/3/4, verdicts still per-cell)

| Group | absolute | percentile | regime | rolling | strict rolling |
|---|--:|--:|--:|--:|--:|
| semi_hardware | +6.2 ✓ | +0.1 n.d. | +5.5 ✓ | +5.9 ✓ | +4.1 ✓ |
| saas | +10.2 ✓ | −3.8 n.d. | +6.1 ✓ | −2.4 n.d. | −3.5 n.d. |
| consumer_internet | +9.5 ✓ | **−7.4 CONTRADICTS** | −3.7 n.d. | **−5.5 CONTRADICTS** | −5.1 n.d. |

**semi_hardware is the only group whose signal survives every drift-corrected
scheme** (+4.1 to +5.9, CI excludes zero in three of four). saas survives only
under the hindsight-using regime scheme. consumer_internet inverts.

#### Recommendation

- **Live tool → scheme D (rolling).** Point-in-time, self-recalibrating, no era
  to guess, and it keeps an absolute-ish judgement. Cost: a warm-up period.
  With a minimum-20-*observations* rule it covers 97.3% of rows; requiring a
  full 20 *quarters* it covers 74.7% and starts 2017-01.
- **Do not ship scheme C (regime).** Its bands are fitted on the whole era
  including its future, and live you would be guessing the current regime — the
  concern raised was correct, and the numbers confirm the advantage is partly
  hindsight.
- **Report scheme B (percentile) alongside any result** as the honesty check.
  It is the only scheme where the 2012-vs-2026 problem cannot arise.

### CVE robustness checks — `backtest_version = '1.2-ROBUST'` (`cve_checks.py`)

**The single most important result of the whole project: the bootstrap CI was
systematically misleading on every unbalanced-leg scheme.** It resamples within
legs, so it cannot see a bias living in cross-group weighting. Every "CONFIRMS"
previously reported under absolute, regime or rolling bands must be re-read
against a shuffled null. Only the percentile scheme has a null at zero by
construction (it assigns exactly k/k labels per group).

**Check 1 — placebo. HARNESS PASSES.** Pooled placebo distribution centred at
**−0.014pp (sd 0.85)** over 500 within-(cell,quarter) label shuffles, so the
spread computation, grouping and forward-return join are sound.
- Real percentile pooled −2.93 sits OUTSIDE the null (p=0.000): the
  cross-sectional result is a small but genuine **inversion**, not an absence.
- saas-2 −14.97 (p=0.000) real inversion · deep_tech-3 +22.59 (p=0.018) ·
  semi_hardware-3 −0.16 (p=0.914) truly null.
- **The rolling-band null is centred at +5.06pp, not zero.** The real
  semi_hardware rolling result (+5.95) sits inside it, **p=0.29**.

**Check 2 — split width.** No tail effect. semi_hardware pooled +0.4 / +0.1 /
−1.9 / −3.6 at halves/terciles/quintiles/deciles, never significant. The saas
inversion deepens monotonically toward the tails (−2.2 → −10.7 pooled; saas-2
−12.4 → −23.0, significant at every width). **deep_tech-3 does not survive**:
+22.6 at terciles but **+0.4 at halves on its largest sample (96/96)** — a
split-width artefact, retracted. Coverage 96.8/93.4/88.9/88.9%.

**Check 3 — sub-period.** semi_hardware null in both halves (−0.2 then +0.6).
consumer_internet existed early and decayed (−12.3 [−18,−6] → −5.2 n.s.).
saas-2 is the exception — it STRENGTHENED (−7.3 → **−18.2 [−28,−6]**). Early
period carries only 45–48% of the panel, so the two halves are not equally
powered.

**Check 4 — out-of-sample. THE SEMICONDUCTOR RESULT COLLAPSES.** A band frozen
on 2012–2019 medians gives OOS +14.3 [+7.7,+20.4] "CONFIRMS" — but legs are
495/1605 (balance 0.31) and its placebo null is centred at **+11.16**
[+6.46,+16.29]. Real excess **+3.19pp, empirical p=0.106**. In-sample it scores
+1.72 against a +3.21 null (p=0.85), i.e. below its own null.

**Verdict: treat the matrix as null for stock selection.** semi_hardware
returned null on four independent tests (percentile, all split widths, both
sub-periods, out-of-sample). The earlier +5.5/+5.9/+4.1 marks were the
leg-imbalance artefact.

**Check 5 — cell correlation.** 17 cells with ≥20 quarters of z-score history.
**Mean off-diagonal pairwise correlation +0.143; only 0.7% of 136 pairs exceed
0.7.** The cells are NOT redundant — they do carry largely independent timing
information, so a cell-level signal is not "a sector signal with extra steps".
Correlation with the sector aggregate ranges +0.008 (consumer_internet-4) to
+0.755 (semi_hardware-3), mean +0.414.
- **Semis do NOT cleanly decouple.** semi_hardware averages +0.335 vs the
  all-cell +0.414, but that average is dragged down by its two thinnest cells
  (semi_hardware-1 +0.010 on 28 quarters, -4 +0.183). The best-populated semi
  cell, **semi_hardware-3, is the MOST sector-correlated cell in the matrix at
  +0.755**. The cycle-independence story is not supported.
- Caveat: the sector aggregate is the median of the cell z-scores, so each
  cell partially self-correlates with it; treat the vs-sector column as an
  upper bound.

**What survives everything:** (1) the **saas-2 inversion** — robust across split
widths, strengthening post-2019, p=0.000 against its null; (2) a **cohort-timing
effect** that is real but is not stock selection.

### SHIPPED — rolling bands + rewritten platform claims (2026-08-16)

**Rolling bands are live.** `build_cell_bands.py` computes, per cell, the median
primary metric over the trailing 20 quarters and writes
`band = 0.7x .. 1.3x median` into a new **`cell_bands`** table, which ships in
the slim DB (`build_repo_db.py` keep-list updated). **17 of 20 cells get a
rolling band; 3 fall back to the static v2 numbers** (deep_tech-4,
hyperscale-1, hyperscale-4 — too thin) and the UI says so.

`valuation_engine.verdict(cell, metrics, bands=None)` takes an optional band
override — still ONE verdict function. A rolling band replaces only the
numbers; the metric, its direction, and the deep_tech-1 pre-revenue switch are
properties of the cell and never move. `app.py` loads bands via
`_get_cell_bands()` (cached 1h) and renders them through `fair_rows_for()`;
only the PRIMARY row is re-centred, secondary qualifiers keep reference ranges.

Selected new bands vs the old static ones:

| Cell | static | rolling | cohort median |
|---|---|---|--:|
| saas-2 | 8–18 | **3.81–7.08** | 5.45 |
| consumer_internet-2 | 4–10 | **1.44–2.67** | 2.05 |
| consumer_internet-3 | 18–30 | **11.37–21.11** | 16.24 |
| semi_hardware-2 | 1.5–4.0 | **1.31–2.44** | 1.88 |
| semi_hardware-3 | 20–35 | **22.50–41.79** | 32.14 |

This also retires the saas-2 ARR band mismatch flagged in Phase 2 — the cell is
now judged against the cohort's own EV/NTM Rev history (5.45x) rather than a
band calibrated for ARR (8–18x) it could never meet.

Every band carries provenance in the UI: *"Fair range = 0.7x-1.3x the cohort's
median of 16.24, measured over 2021-10-01 to 2026-07-01 (913 observations). It
re-centres each quarter as the cohort re-rates."*

**Claims rewritten** in `app.py` (About page x3 sections, interpretation
disclaimer) and `backtest_page.py` (limitations panel):
- The tool states plainly that it shows **where a cohort sits versus its own
  history**, and that it **does not** identify which stock within a cohort to
  buy — naming the evidence (no advantage at any split width, either
  sub-period, or out-of-sample).
- The Backtest page leads its limitations with the failed cross-sectional
  claim and with the bootstrap/unbalanced-legs caveat, so nobody reads the
  portfolio numbers without it.

**Not pushed** — local only. Run `build_cell_bands.py` then `build_repo_db.py`
after any panel rebuild, or the shipped bands go stale.

### Two product fixes (2026-08-16)

**1. Verdict SUPPRESSED in `saas-2` and `consumer_internet-2`.** The evidence
says a low multiple there signals decelerating growth, not value (saas-2 -15pp
at terciles / -23pp at deciles, p=0.000, strengthening post-2019;
consumer_internet-2 -10.3pp). `valuation_engine.SUPPRESSED_CELLS` withholds the
cheap/expensive judgement and states why; the multiple is still shown. **Not
inverted** — one robust negative is not a tradable positive. `build_panel.py`
passes `respect_suppression=False` so the research record keeps measuring it.
A third inline copy of the verdict rule in `app.py` (line ~3216) was routed
through the engine at the same time — it would have bypassed the suppression.

**2. `it_services` category added; 32 tickers moved out of the `deep_tech`
residual bucket** (`reclassify_it_services.py`), including JKHY, ACIW, LDOS,
CACI, ACN, CTSH. They carry an honest label and **no verdict** — the engine has
five calibrated models and none fit labour-based services, so inventing a band
would swap a false label for a false number. Membership is AV's own
`industry = INFORMATION TECHNOLOGY SERVICES` plus explicit adds/holds; the raw
tag over-reached and was blocked from moving IBM (a deliberate override), APLD
(data centres), CIFR/CORZ (bitcoin/HPC) and INOD. Panel 17,291 -> 16,029 rows
(1,262 it_services stock-quarters now have no cell, by design).
Category lists in `app.py` now come from `_ve.CATEGORY_ORDER/CATEGORY_LABELS` —
a hardcoded 5-item list had already hidden the new group from the universe grid
while the footer counted 6.

**Three follow-on defects the reclassification exposed, all fixed:**
- The suppression notice was nested inside `if fair_rows:`. A no-model category
  has NO fair rows, so an IT-services user saw a blank panel and no explanation
  — the one case that most needs one. Moved out of that block.
- **`valuations.matrix_cell` went stale**: `classifications` said
  `it_services-4`, `valuations` still said `deep_tech-4`, so the app would
  render the OLD cell's fair ranges for a renamed name. Synced (32 rows), and
  **new integrity test R6** now fails if the two tables ever disagree again.
- **R5 was re-deriving panel verdicts with suppression ON** while the panel
  stores them with it OFF, producing 10 false blanks. Both the blank check and
  the panel re-derivation now pass `respect_suppression=False`, and R5 gained a
  positive assertion that suppression still bites on the product path (so
  deleting `SUPPRESSED_CELLS` fails the gate rather than passing silently).

**3. `deep_tech` relabelled → "Mixed / Pre-Revenue"** (2026-08-16). After the
it_services move the bucket still held **48 tickers across 14 AV industries** —
13 application software, 8 infrastructure software, 7 semiconductors, 4 hardware,
4 aerospace, plus utilities, internet retail and financial data. Genuine deep
tech is a minority. The rule is literally "no dominant revenue stream >=40%, or
pre-revenue", so the label now states that. `CATEGORY_NOTES` surfaces a note on
the Analysis page and the About copy explains it is a residual bucket, not a
thesis.

**The internal key stays `deep_tech` deliberately** — it is embedded in
matrix_cell strings across `classifications`, `valuations`, `backtest_panel`,
`cell_bands`, `backtest_cell_stats` and every stored 1.1-RR / 1.2-ROBUST
result. Renaming it would orphan all of that for a cosmetic gain; the label is
what users read, and the label is now accurate.

### Files added this session
`build_panel.py` · `analyze_test1.py` · `classify_delisted_offline.py` ·
`to_classify.txt` · `test1_cell_stats.csv` · `classify_backtest_universe.log` ·
`analyze_test2.py` · `test2_transitions.csv` · `portfolio_sim.py` ·
`price_integrity_check.py` · `Valuora_Previous_UI/backtest_page.py` ·
`integrity_check.py` · `integrity_failures.csv` · `integrity_run.log` ·
`phase1_clean.py` · `valuation_engine.py` · `analyze_test1_rr.py` ·
`test1_rr_cell_stats.csv` · `test1_rr_run.log` ·
`analyze_bands.py` · `band_variants.csv` · `band_variants.log` ·
`cve_checks.py` · `check1_placebo.csv/.log` · `check2_splits.csv/.log` ·
`check3_subperiod.csv/.log` · `check4_oos.csv/.log` · `check4_null.log` ·
`check5_correlation.csv/.log` · `build_cell_bands.py`

---

## Metrics Accuracy Rebuild (2026-08-11) — `refresh_metrics.py`

All 457 classified tickers were recomputed on AV Pro data. **78 tickers had a
market cap wrong by ≥10%**; the largest corrections were PANW $200bn→$284bn,
PLTR $326bn→$453bn, SHOP $142bn→$199bn, ORCL $649bn→$426bn, ASTS $41bn→$20bn.

### Root cause 1 — yfinance was the market-cap source (now AV-first)
Two independent errors, both live in every EV multiple, P/E and FCF yield:
1. **Dual-class undercount.** `yfinance.sharesOutstanding` returns only the
   listed class: TEAM 160M vs 253M actual, WDAY 204M vs 254M, NET 320M vs 354M.
   An understated market cap makes EV/Revenue look cheap and FCF yield look
   high, so the engine was **systematically biased toward "undervalued" for
   every dual-class company**.
2. **Staleness.** The stored `market_cap` was frozen at whatever price applied
   when the pipeline last ran — SNOW sat at $57.9bn against a live $115.1bn.

`_resolve_market_cap()` now computes market cap as *latest close × AV
`commonStockSharesOutstanding`* (counts all classes, tracks `price_data` so it
cannot go stale), then reconciles against AV OVERVIEW's own
`MarketCapitalization`. Agreement within 25% → use the price-consistent value;
disagreement → trust OVERVIEW, back out implied shares, and log to
`mcap_review.tsv`. **AV is not universally right either** — for some ADRs and
post-reverse-split names its balance-sheet share count is wrong by orders of
magnitude (SIMO, TDTH, XPER), which is exactly what the reconciliation catches.
17 tickers currently land in the conflict path. yfinance survives only as a
last-resort fallback plus the currency gate and deferred revenue.

### Root cause 2 — two genuine AV statement defects (fixed)
Validated first: AV statements match yfinance **quarter-by-quarter within 2% on
24 of 28** sampled tickers, and 0.0% on MSFT/NVDA/AAPL/SNOW/ABNB/CRM/ADBE/INTC/
AMD/NFLX. **AV is also fresher** — 17 of 29 sampled tickers had a completed
quarter AV had filed and yfinance had not. (A first pass appeared to show TEAM
36.8% off; that was a defect in the comparison — yfinance's newest column was
empty and pandas summed the NaN as 0. AV was right.)

1. **Corrupt `grossProfit`** — 7 tickers / 11 quarters. AV ships a wrong gross
   profit in the newest quarter while revenue and COGS stay correct, in three
   shapes: gp == revenue with non-zero COGS (**TTD: 100% GM instead of 76.9%**),
   gp negative while revenue > COGS (**XPER: −$88M instead of +$328M LTM**), and
   gp missing (MBGL). `revenue − COGS` reproduces yfinance exactly, so
   `_gross_profit_ltm()` falls back to it and logs every substitution. This
   matters disproportionately: gross margin gates the SaaS (≥65%), hyperscale
   (≥55%) and semi_hardware (<55%) validators **and** carries 15% weight in
   every FH stage, so one bad value can misclassify both axes.
2. **Semi-annual filers double-counted** — EPWK and WSE file ~182-day periods,
   so summing 4 booked **two years** of revenue as LTM. `_periods_per_year()`
   now sizes every LTM/prior window off measured filing spacing (4 / 2 / 1).

### Not a defect — do not "fix" this
112 tickers report a different `netIncome` in INCOME_STATEMENT vs CASH_FLOW.
That is correct accounting: the income statement reports income attributable to
the parent, the cash flow starts from consolidated income including minority
interests (ACN's $48.9M gap is exactly its NCI). The engine already uses the
income statement for NI/EPS/P-E and the cash flow only for OCF, capex, SBC and
D&A, so each is used correctly.

### ⚠ Stale-row hazard (fixed, but the app is still fragile)
`computed_metrics` and `valuations` are **current-state tables, not history**.
Because the backfill advanced many tickers to a newer fiscal quarter, the
refresh wrote a second row per ticker — and **every app.py query against both
tables is a bare `WHERE ticker=?` with no `ORDER BY`**, so the UI would have
served the *stale* row and silently undone the whole correction (83 valuation
rows were affected). `refresh_metrics.py` now prunes both tables to the newest
`as_of_date` per ticker. **The real fix is to add `ORDER BY as_of_date DESC
LIMIT 1` to those queries in `app.py`** — see Known Open Items.

### Residual issues
- `mcap_review.tsv` — 17 tickers where AV's two share sources disagree >25%.
  Worth a manual pass; several (QNT 87%, CBRS 72%, TDTH 100%) are large enough
  that neither source is obviously right.
- MBGL reports zero `costOfRevenue`, so its gross margin computes to 100%. AV
  has no COGS for it; flagged rather than fabricated.
- 4 of 28 sampled tickers diverge from yfinance on older quarters (OSS, FTV,
  PPLI, FIS) — spinoff/divestiture restatements, where AV holds the
  as-originally-reported basis. OSS is a **yfinance** error (it reports negative
  revenue for 2025-12-31); AV is correct there.
- **BM categories were deliberately NOT re-run.** No BM validator reads market
  cap, so the fix cannot legitimately change a BM result, but the Gemini
  tiebreaker is non-deterministic and re-running it would churn categories and
  desync `backtest_panel`'s static BM. FH stage and all valuations were rebuilt.
- **`backtest_panel` is unaffected** — `build_panel.py` already used AV share
  counts, so Test 1's results stand.

---

## Known Open Items

| Item | Priority | Notes |
|------|----------|-------|
| **Hyperscale category split — DEFERRED** | Decided | Splitting `hyperscale` into *pure cloud infrastructure* (MSFT/GOOGL/AMZN/CRWV/NBIS — usage-based compute, extreme CapEx) vs *diversified mega-cap* (IBM/ORCL — mixed legacy services + cloud) was considered and **deliberately deferred until after the backtest**. The two groups have genuinely different CapEx intensity and margin structure, and an 8-ticker category spanning both is doing real work to hold them together. But re-cutting the taxonomy now would invalidate the historical backfill: `fh_stage_history`, `stage_change_log` and every stored `valuations` row are keyed to the current `matrix_cell` strings, so a split means re-running the whole backfill against a moving target. **Backtest against the frozen v2 taxonomy first, then split with a `MATRIX_VERSION` bump and one clean re-run.** |
| ~~Rule 1 not implemented~~ | **DONE 2026-08-11** | Implemented at two layers (ingest truncation in `backfill_fundamentals.py`, read guard in `backtester.py::get_price_on_date`) and verified: DB-wide assertion returns 0 rows past any delisting date; SPLK ends exactly 2024-03-15 with 153 frozen bars discarded. Full results in `BACKTEST_RULES.md`. |
| **`reported_date <= fiscal_date` on 554 rows** | **High** | A quarter cannot be filed before it ends; 471 rows sit on panel tickers. The panel's `COALESCE(reported_date, fiscal_date+45d)` gate trusts these, admitting filings ~30-45 days before they were public. Fix: treat `reported_date <= fiscal_date` as NULL so the +45d fallback applies. Found by `integrity_check.py` T1. |
| **Mid-series currency changes (NBIS/MIXT/LN)** | **High** | The B6 gate reads yfinance's *current* `financialCurrency` and cannot see a currency change within a ticker's history. NBIS mixes RUB:173 / USD:22 and is 1 of 8 hyperscale names. Fix: gate on per-row `reportedCurrency` from the raw JSON. Found by `integrity_check.py` F5. |
| **Splits corrupt stored price history** | **High** | `price_data` is back-adjusted but written `INSERT OR IGNORE`, so a split leaves the series spliced across two scales (KLAC 10x, CRWD 4x — both repaired 2026-08-11). Detector + repair: `price_integrity_check.py [--fix]`. **Run it after every backfill.** Real fix = re-write history on split detection instead of ignoring existing rows. Delisted names (AV-sourced) cannot be checked this way. |
| **Backtest #1 pass conditions: only #1 passes** | **High** | Portfolio loses to QQQ and to the equal-weight universe at every concentration; Test 2 is a clean negative. Fix the rules before re-running — see the handoff header. |
| **`app.py` reads `computed_metrics` / `valuations` with no `ORDER BY`** | **High** | Every query is a bare `WHERE ticker=?`. Both tables can legitimately hold more than one row per ticker (a new fiscal quarter writes a new `as_of_date`), and SQLite will then serve whichever row it finds first — the stale one. `refresh_metrics.py` prunes as a stopgap; the fix is `ORDER BY as_of_date DESC LIMIT 1` at app.py lines 196, 202, 1370, 2883, 2941, 2947, 3236, 3839. |
| **`ev_ntm_arr` key bug — 7 cells show no verdict in the app** | **High** | `FAIR_RANGES_FULL` keys the "EV/NTM Rev" cells on `ev_ntm_arr`, which the pipeline only writes for ARR-routed cells; the S1 cells route to methods computing neither key. Fix = point those cells at `ev_ntm_revenue` and re-route the S1 cells in `VALUATION_MATRIX`. `build_panel.py` already works around it. Details in *Backtest #1* above. |
| **`run_universe.py` aborts on delisted tickers** | Medium | It fetches yfinance prices before classifying; yfinance serves zero bars for delisted symbols, so the ticker is skipped even when `price_data` already holds AV-backfilled prices. Worked around by `classify_delisted_offline.py`. Real fix = skip the price fetch when local prices already cover the ticker. |
| **Verdict bands drift with the multiple cycle** | Medium | 75.6% of 2012 panel rows read "undervalued" vs 43.1% in 2026 — absolute bands vs a decade of multiple expansion. Regime-conditioning the fair ranges is the fix; see the Rate-regime item below. |
| Data gaps in 11 `in_backtest` names | Medium | `backtest_data_gaps.tsv`: 9 missing a statement type (recent micro-cap IPOs — BRAI, BSP, BVC, CHRN, CSQR, IQMX, QNT, WSE, STEC), 2 with statements but no prices (ANSS acquired by Synopsys; OZON no yfinance coverage). They drop out naturally at panel build — `compute_point_in_time_metrics` returns None without all three statements — but should be reported as an exclusion count, not silently lost. |
| Backtest window hardcoded to 5y | Medium | `backtester.py:206` uses `end - 365*5` although fundamentals now reach 2006 and prices 1962. Widen to use the deeper history. |
| Supabase RLS disabled | High | RLS is off on `users`, `watchlist`, `stage_change_log` so the anon key can write. Re-enable with per-user policies before real users. |
| yfinance commercial use | High | Must migrate to licensed price data before production |
| Empty matrix cells | Medium | hyperscale-4, saas-1, semi_hardware-1, deep_tech-4 have no occupants — ranges untested |
| MRVL / INTC cycle P/E | Medium | Add `MANUAL_CYCLE_PE_EXCLUSIONS` guard for `net_income_quality_flag` tickers; extreme values distort cell medians |
| `rpo_prior_year` coverage | Low | NULL for several tickers; self-resolves next extraction cycle |
| Non-USD AV credits | Low | 3 AV credits burned before the currency gate fires (TSM/ASML/SPOT/BABA). Cheap on Pro, but add a pre-fetch currency check |
| Sentiment classifier (C3) | Future | Not yet implemented; placeholder in architecture |
| FH weight sensitivity | Future | Sensitivity-test S3/S4 FCF boundary ±3pp on historical data |
| Rate-regime conditioning | Future | Thresholds calibrated on 2010–2024 low-rate era |
| `google.generativeai` deprecation | Low | Migrate to the `google.genai` package |
